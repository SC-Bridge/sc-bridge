-- AUTO-GENERATED by test/generate-test-schema.sh — DO NOT EDIT BY HAND.
-- Consolidated test DB baseline (Better Auth + migrations + seed).
-- migrations: 262
-- Regenerate after adding/changing a migration: npm run test:schema:gen
-- ── tables ──
CREATE TABLE "user" (id TEXT PRIMARY KEY NOT NULL, name TEXT NOT NULL, email TEXT NOT NULL UNIQUE, emailVerified INTEGER NOT NULL DEFAULT 0, image TEXT, createdAt TEXT NOT NULL, updatedAt TEXT NOT NULL, role TEXT DEFAULT 'user', banned INTEGER DEFAULT 0, banReason TEXT, banExpires TEXT, status TEXT NOT NULL DEFAULT 'active', deleted_at TEXT NULL, gravatar_opted_out INTEGER NOT NULL DEFAULT 0, primary_org_id TEXT, twoFactorEnabled INTEGER DEFAULT 0);
CREATE TABLE "session" (id TEXT PRIMARY KEY NOT NULL, expiresAt TEXT NOT NULL, token TEXT NOT NULL UNIQUE, createdAt TEXT NOT NULL, updatedAt TEXT NOT NULL, ipAddress TEXT, userAgent TEXT, userId TEXT NOT NULL REFERENCES "user"(id), activeOrganizationId TEXT, impersonatedBy TEXT);
CREATE TABLE "account" (id TEXT PRIMARY KEY NOT NULL, accountId TEXT NOT NULL, providerId TEXT NOT NULL, userId TEXT NOT NULL REFERENCES "user"(id), accessToken TEXT, refreshToken TEXT, idToken TEXT, accessTokenExpiresAt TEXT, refreshTokenExpiresAt TEXT, scope TEXT, password TEXT, createdAt TEXT NOT NULL, updatedAt TEXT NOT NULL);
CREATE TABLE "verification" (id TEXT PRIMARY KEY NOT NULL, identifier TEXT NOT NULL, value TEXT NOT NULL, expiresAt TEXT NOT NULL, createdAt TEXT, updatedAt TEXT);
CREATE TABLE "organization" (id TEXT PRIMARY KEY NOT NULL, name TEXT NOT NULL, slug TEXT NOT NULL UNIQUE, logo TEXT, createdAt TEXT NOT NULL, metadata TEXT, description TEXT, verification_key TEXT, verified_at TEXT, verified_by TEXT, rsi_model TEXT, rsi_commitment TEXT, rsi_roleplay TEXT, rsi_primary_focus TEXT, rsi_secondary_focus TEXT, rsi_banner_url TEXT, rsi_member_count INTEGER, rsi_history_html TEXT, rsi_manifesto_html TEXT, rsi_charter_html TEXT, last_synced_at TEXT, rsiSid TEXT, rsiUrl TEXT, homepage TEXT, discord TEXT, twitch TEXT, youtube TEXT);
CREATE TABLE "member" (id TEXT PRIMARY KEY NOT NULL, organizationId TEXT NOT NULL REFERENCES "organization"(id), userId TEXT NOT NULL REFERENCES "user"(id), role TEXT NOT NULL DEFAULT 'member', createdAt TEXT NOT NULL);
CREATE TABLE "invitation" (id TEXT PRIMARY KEY NOT NULL, organizationId TEXT NOT NULL REFERENCES "organization"(id), email TEXT NOT NULL, role TEXT, status TEXT NOT NULL DEFAULT 'pending', expiresAt TEXT NOT NULL, inviterId TEXT NOT NULL REFERENCES "user"(id), createdAt TEXT, teamId TEXT);
CREATE TABLE "twoFactor" (id TEXT PRIMARY KEY NOT NULL, secret TEXT NOT NULL, backupCodes TEXT NOT NULL, userId TEXT NOT NULL REFERENCES "user"(id), verified INTEGER DEFAULT 1);
CREATE TABLE "passkey" (id TEXT PRIMARY KEY NOT NULL, name TEXT, publicKey TEXT NOT NULL, userId TEXT NOT NULL REFERENCES "user"(id), webauthnUserID TEXT, counter INTEGER NOT NULL DEFAULT 0, deviceType TEXT, backedUp INTEGER, transports TEXT, credentialID TEXT NOT NULL, createdAt TEXT, aaguid TEXT);
CREATE TABLE vehicle_types (
    id INTEGER PRIMARY KEY,
    key TEXT UNIQUE NOT NULL,
    label TEXT NOT NULL
);
CREATE TABLE insurance_types (
    id INTEGER PRIMARY KEY,
    key TEXT UNIQUE NOT NULL,
    label TEXT NOT NULL,
    duration_months INTEGER,
    is_lifetime BOOLEAN NOT NULL DEFAULT FALSE
);
CREATE TABLE sync_sources (
    id INTEGER PRIMARY KEY,
    key TEXT UNIQUE NOT NULL,
    label TEXT NOT NULL
);
CREATE TABLE production_statuses (
    id INTEGER PRIMARY KEY,
    key TEXT UNIQUE NOT NULL,
    label TEXT NOT NULL
);
CREATE TABLE game_versions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT UNIQUE NOT NULL,
    code TEXT UNIQUE NOT NULL,
    channel TEXT,
    is_default BOOLEAN DEFAULT FALSE,
    released_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
, build_number TEXT);
CREATE TABLE sync_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    source_id INTEGER NOT NULL REFERENCES sync_sources(id),
    endpoint TEXT,
    status TEXT NOT NULL,
    record_count INTEGER DEFAULT 0,
    error_message TEXT,
    started_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    completed_at DATETIME
);
CREATE TABLE app_settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
CREATE TABLE change_event_types (
    id INTEGER PRIMARY KEY,
    key TEXT UNIQUE NOT NULL,
    label TEXT NOT NULL,
    category TEXT NOT NULL
);
CREATE TABLE user_rsi_profile (
  user_id       TEXT PRIMARY KEY REFERENCES user(id) ON DELETE CASCADE,
  handle        TEXT NOT NULL,
  display_name  TEXT,
  citizen_record TEXT,
  enlisted_at   TEXT,
  avatar_url    TEXT,
  main_org_slug TEXT,
  orgs_json     TEXT,   -- JSON: [{slug, name, is_main}]
  fetched_at    TEXT NOT NULL DEFAULT (datetime('now'))
, verified_at TEXT, verified_handle TEXT);
CREATE TABLE paint_vehicles (
    paint_id   INTEGER NOT NULL REFERENCES paints(id),
    vehicle_id INTEGER NOT NULL REFERENCES vehicles(id), [id] INTEGER, [paint_uuid] TEXT, [vehicle_class_name] TEXT, is_default_for_vehicle INTEGER DEFAULT 0,
    PRIMARY KEY (paint_id, vehicle_id)
);
CREATE TABLE vehicle_loaners (
    vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
    loaner_id  INTEGER NOT NULL REFERENCES vehicles(id),
    PRIMARY KEY (vehicle_id, loaner_id)
);
CREATE TABLE vehicle_images (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    vehicle_id  INTEGER NOT NULL REFERENCES vehicles(id) ON DELETE CASCADE,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    cf_images_id TEXT, [rsi_cdn_new] TEXT, [rsi_cdn_old] TEXT, [rsi_graphql] TEXT, [rsi_id] INTEGER, [rsi_slug] TEXT,
    UNIQUE(vehicle_id)
);
CREATE TABLE vehicle_images_archive (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    vehicle_id  INTEGER NOT NULL REFERENCES vehicles(id) ON DELETE CASCADE,
    rsi_id      INTEGER,
    rsi_slug    TEXT,
    rsi_cdn_new TEXT,
    rsi_cdn_old TEXT,
    rsi_graphql TEXT,
    archived_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE invite_tokens (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  token      TEXT NOT NULL UNIQUE,
  created_at TEXT DEFAULT (datetime('now')),
  used_at    TEXT
);
CREATE TABLE user_fleet (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id           TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    vehicle_id        INTEGER NOT NULL REFERENCES vehicles(id),
    insurance_type_id INTEGER REFERENCES insurance_types(id),
    warbond           BOOLEAN NOT NULL DEFAULT FALSE,
    is_loaner         BOOLEAN NOT NULL DEFAULT FALSE,
    pledge_id         TEXT,
    pledge_name       TEXT,
    pledge_cost       TEXT,
    pledge_date       TEXT,
    custom_name       TEXT,
    equipped_paint_id INTEGER REFERENCES paints(id),
    imported_at       DATETIME DEFAULT CURRENT_TIMESTAMP,
    org_visibility    TEXT    NOT NULL DEFAULT 'private',
    available_for_ops INTEGER NOT NULL DEFAULT 0
);
CREATE TABLE user_llm_configs (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id           TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    provider          TEXT    NOT NULL,
    encrypted_api_key TEXT    NOT NULL,
    model             TEXT,
    created_at        DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at        DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, provider)
);
CREATE TABLE user_settings (
    id      INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    key     TEXT    NOT NULL,
    value   TEXT    NOT NULL,
    UNIQUE(user_id, key)
);
CREATE TABLE ai_analyses (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id       TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    provider      TEXT    NOT NULL,
    model         TEXT    NOT NULL,
    vehicle_count INTEGER NOT NULL,
    analysis      TEXT    NOT NULL,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE mission_type_givers (
  mission_type_id  INTEGER NOT NULL REFERENCES mission_types(id),
  mission_giver_id INTEGER NOT NULL REFERENCES mission_givers(id),
  PRIMARY KEY (mission_type_id, mission_giver_id)
);
CREATE TABLE rsi_media (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    slug             TEXT    NOT NULL UNIQUE,   -- e.g. 'am12jusvbx8mqr'
    filename         TEXT,                      -- e.g. 'Dragonfly-Left'
    source_extension TEXT,                      -- e.g. 'jpg', 'png'
    cdn_format       TEXT    NOT NULL DEFAULT 'old',  -- 'old' (/media/) or 'new' (media.rsi.com)
    cf_image_id      TEXT,                      -- CF Images ID (null until uploaded)
    cf_image_url     TEXT,                      -- CF delivery URL (null until uploaded)
    first_seen_at    TEXT    NOT NULL DEFAULT (datetime('now')),
    last_seen_at     TEXT    NOT NULL DEFAULT (datetime('now'))
);
CREATE TABLE vehicle_rsi_meta (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    vehicle_id     INTEGER NOT NULL UNIQUE REFERENCES vehicles(id) ON DELETE CASCADE,
    rsi_ship_id    INTEGER,            -- RSI ship matrix ID (from /ship-matrix/index)
    rsi_media_id   INTEGER REFERENCES rsi_media(id),
    rsi_store_url  TEXT,               -- e.g. '/pledge/ships/rsi-aurora/Aurora-ES'
    first_seen_at  TEXT    NOT NULL DEFAULT (datetime('now')),
    last_seen_at   TEXT    NOT NULL DEFAULT (datetime('now'))
);
CREATE TABLE paint_rsi_meta (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    paint_id       INTEGER NOT NULL UNIQUE REFERENCES paints(id) ON DELETE CASCADE,
    rsi_media_id   INTEGER REFERENCES rsi_media(id),
    first_seen_at  TEXT    NOT NULL DEFAULT (datetime('now')),
    last_seen_at   TEXT    NOT NULL DEFAULT (datetime('now'))
);
CREATE TABLE rsi_entity_mappings (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    rsi_name       TEXT    NOT NULL,           -- what RSI calls it, e.g. 'Hurricane'
    rsi_item_id    INTEGER,                    -- RSI's internal item ID (from upgrade data, ship matrix)
    rsi_kind       TEXT    NOT NULL,           -- 'Ship','Insurance','Skin','FPS Equipment',
                                              -- 'Hangar decoration','Component','Credits'
    entity_table   TEXT,                       -- target table: 'vehicles','paints','fps_weapons', etc.
    entity_id      INTEGER,                    -- row ID in the target table (null until mapped)
    rsi_media_id   INTEGER REFERENCES rsi_media(id),
    reviewed_at    TEXT,                       -- null = pending review
    reviewed_by    TEXT,                       -- admin user_id (informational, not FK — admin deletion must not break mappings)
    created_at     TEXT    NOT NULL DEFAULT (datetime('now')),
    UNIQUE(rsi_name, rsi_kind)
);
CREATE TABLE rsi_staging_items (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    rsi_item_id       INTEGER,                 -- RSI's internal item ID (if known)
    name              TEXT    NOT NULL,
    kind              TEXT,                     -- Ship, Skin, FPS Equipment, etc.
    manufacturer_code TEXT,
    manufacturer_name TEXT,
    image_url         TEXT,                     -- raw URL as seen in hangar HTML
    rsi_media_id      INTEGER REFERENCES rsi_media(id),
    source_user_id    TEXT,                     -- who surfaced this (debug only, not displayed)
    discovered_at     TEXT    NOT NULL DEFAULT (datetime('now')),
    reviewed_at       TEXT,
    review_action     TEXT,                     -- 'promoted','discarded','deferred'
    promoted_to_table TEXT,                     -- e.g. 'vehicles','paints'
    promoted_to_id    INTEGER,
    notes             TEXT,
    UNIQUE(name, kind)
);
CREATE TABLE rsi_staging_media (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    slug             TEXT    NOT NULL,
    filename         TEXT,
    source_extension TEXT,
    cdn_format       TEXT,
    image_url        TEXT,                     -- full URL as seen
    source_context   TEXT,                     -- e.g. 'Ship: Terrapin Medic', 'Skin: Stormcloud'
    discovered_at    TEXT    NOT NULL DEFAULT (datetime('now')),
    reviewed_at      TEXT,
    review_action    TEXT,                     -- 'promoted','discarded'
    UNIQUE(slug)
);
CREATE TABLE rsi_staging_reports (
    id                        INTEGER PRIMARY KEY AUTOINCREMENT,
    generated_at              TEXT    NOT NULL DEFAULT (datetime('now')),
    report_json               TEXT    NOT NULL,  -- structured diff
    new_items_count           INTEGER NOT NULL DEFAULT 0,
    new_media_count           INTEGER NOT NULL DEFAULT 0,
    new_insurance_types_count INTEGER NOT NULL DEFAULT 0,
    emailed_at                TEXT,               -- null until notification sent
    reviewed_at               TEXT
);
CREATE TABLE user_hangar_syncs (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id      TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    synced_at    TEXT    NOT NULL DEFAULT (datetime('now')),
    source       TEXT    NOT NULL DEFAULT 'extension',  -- 'extension','hangarxplor'
    pledge_count INTEGER,
    ship_count   INTEGER,
    item_count   INTEGER,
    raw_hash     TEXT                                    -- hash of import payload for dedup
);
CREATE TABLE user_pledges (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id             TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    sync_id             INTEGER NOT NULL REFERENCES user_hangar_syncs(id) ON DELETE CASCADE,
    rsi_pledge_id       INTEGER NOT NULL,           -- RSI's pledge ID
    name                TEXT    NOT NULL,            -- 'Standalone Ships - Constellation Taurus'
    value               TEXT,                        -- '$220.00 USD' (raw string)
    value_cents         INTEGER,                     -- 22000 (parsed)
    configuration_value TEXT,                        -- '$0.00 USD'
    currency            TEXT,                        -- 'Store Credit', 'New Money'
    pledge_date         TEXT,                        -- 'August 19, 2025' (raw)
    pledge_date_parsed  TEXT,                        -- '2025-08-19' (ISO)
    is_upgraded         INTEGER NOT NULL DEFAULT 0,
    is_reclaimable      INTEGER NOT NULL DEFAULT 0,
    is_giftable         INTEGER NOT NULL DEFAULT 0,
    availability        TEXT,                        -- 'Attributed', 'Gift'
    UNIQUE(user_id, rsi_pledge_id)
);
CREATE TABLE user_pledge_items (
    id                    INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id               TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    user_pledge_id        INTEGER NOT NULL REFERENCES user_pledges(id) ON DELETE CASCADE,
    title                 TEXT    NOT NULL,           -- 'Terrapin Medic'
    kind                  TEXT,                       -- 'Ship','Insurance','Skin','FPS Equipment',
                                                     -- 'Hangar decoration','Component','Credits'
    manufacturer_code     TEXT,                       -- 'ANVL'
    manufacturer_name     TEXT,                       -- 'Anvil Aerospace'
    image_url             TEXT,                       -- raw RSI image URL
    rsi_media_id          INTEGER REFERENCES rsi_media(id),
    rsi_entity_mapping_id INTEGER REFERENCES rsi_entity_mappings(id),
    custom_name           TEXT,                       -- user's ship name (e.g. 'Jean-Luc')
    serial                TEXT,                       -- ship serial number
    is_nameable           INTEGER NOT NULL DEFAULT 0,
    insurance_type_id     INTEGER REFERENCES insurance_types(id),  -- parsed from title for Insurance items
    sort_order            INTEGER NOT NULL DEFAULT 0
);
CREATE TABLE user_pledge_upgrades (
    id                    INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id               TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    user_pledge_id        INTEGER NOT NULL REFERENCES user_pledges(id) ON DELETE CASCADE,
    upgrade_rsi_pledge_id INTEGER,                   -- RSI's upgrade pledge ID (e.g. #21280683)
    upgrade_name          TEXT    NOT NULL,           -- 'Upgrade - Carrack w/C8X to Carrack Expedition...'
    applied_at            TEXT,                       -- 'Feb 28 2020, 7:41 am' (raw)
    applied_at_parsed     TEXT,                       -- '2020-02-28T07:41:00' (ISO)
    new_value             TEXT,                       -- '$295.00 USD' (raw)
    new_value_cents       INTEGER,                    -- 29500 (parsed)
    sort_order            INTEGER NOT NULL DEFAULT 0  -- 0 = most recent upgrade
, [name] TEXT, [rsi_pledge_id] INTEGER, [synced_at] TEXT);
CREATE TABLE user_account_snapshots (
    id                    INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id               TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    sync_id               INTEGER NOT NULL REFERENCES user_hangar_syncs(id) ON DELETE CASCADE,
    synced_at             TEXT    NOT NULL DEFAULT (datetime('now')),
    nickname              TEXT,
    displayname           TEXT,
    avatar_url            TEXT,
    enlisted_since        TEXT,                       -- '2016-01-04 22:37:31'
    country               TEXT,
    concierge_level       TEXT,                       -- 'Wing Commander'
    concierge_next_level  TEXT,                       -- 'Praetorian'
    concierge_progress    INTEGER,                    -- 95 (percentage)
    subscriber_type       TEXT,                       -- 'Imperator'
    subscriber_frequency  TEXT,                       -- 'Monthly'
    store_credit_cents    INTEGER,                    -- 0
    uec_balance           INTEGER,                    -- 50000
    rec_balance           INTEGER,                    -- 3613280
    org_name              TEXT,                       -- 'The Exelus Corporation'
    org_sid               TEXT,                       -- 'EXLS'
    badges_json           TEXT,                       -- JSON map of badge_id → badge_name
    referral_code         TEXT,
    has_game_package      INTEGER
);
CREATE TABLE user_named_ships (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    sync_id         INTEGER NOT NULL REFERENCES user_hangar_syncs(id) ON DELETE CASCADE,
    membership_id   INTEGER NOT NULL,               -- RSI's membership_id (links across pledges)
    default_name    TEXT    NOT NULL,                -- 'Carrack Expedition with Pisces Expedition'
    custom_name     TEXT    NOT NULL,                -- 'Jean-Luc'
    UNIQUE(user_id, membership_id)
);
CREATE TABLE user_change_history (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id       TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    event_type_id INTEGER NOT NULL REFERENCES change_event_types(id),
    provider_id   TEXT,
    field_name    TEXT,
    old_value     TEXT,
    new_value     TEXT,
    metadata      TEXT,
    ip_address    TEXT,
    created_at    DATETIME DEFAULT CURRENT_TIMESTAMP
);
CREATE TABLE npc_factions (
  id       INTEGER PRIMARY KEY AUTOINCREMENT,
  code     TEXT NOT NULL,
  name     TEXT NOT NULL, [key] TEXT, [slug] TEXT,
  UNIQUE(code)
);
CREATE TABLE npc_loadout_items (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  loadout_id        INTEGER NOT NULL REFERENCES npc_loadouts(id) ON DELETE CASCADE,
  port_name         TEXT NOT NULL,
  item_name         TEXT NOT NULL,
  tag               TEXT,
  parent_port       TEXT,
  loot_map_uuid     TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id)
, is_hidden INTEGER DEFAULT 0, resolved_name TEXT, loot_item_id INTEGER, manufacturer_name TEXT, [item_uuid] TEXT, [loadout_uuid] TEXT, [slot] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE loot_item_locations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  loot_map_id INTEGER NOT NULL REFERENCES loot_map(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  source_type TEXT NOT NULL CHECK(source_type IN ('container','shop','npc','contract')),
  location_key TEXT NOT NULL,
  -- Container fields
  container_type TEXT,
  per_container REAL,
  per_roll REAL,
  rolls INTEGER,
  loot_table TEXT,
  -- Shop fields
  actor TEXT,
  faction TEXT,
  slot TEXT,
  probability REAL,
  -- Contract fields
  weight REAL
, spawn_locations TEXT, [item_uuid] TEXT, [location_label] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE user_rsi_profiles (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL UNIQUE,
    rsi_handle TEXT,
    rsi_displayname TEXT,
    avatar_url TEXT,
    enlisted_since TEXT,
    country TEXT,
    concierge_level TEXT,
    subscriber_type TEXT,
    subscriber_frequency TEXT,
    store_credit_cents INTEGER,
    uec_balance INTEGER,
    rec_balance INTEGER,
    referral_code TEXT,
    has_game_package INTEGER DEFAULT 0,
    orgs_json TEXT,
    badges_json TEXT,
    featured_badges_json TEXT,
    synced_at TEXT NOT NULL DEFAULT (datetime('now')),
    FOREIGN KEY (user_id) REFERENCES user(id)
);
CREATE TABLE user_buyback_pledges (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,
    rsi_pledge_id INTEGER NOT NULL,
    name TEXT NOT NULL,
    value_cents INTEGER,
    date TEXT,
    is_credit_reclaimable INTEGER DEFAULT 0,
    items_json TEXT,
    synced_at TEXT NOT NULL DEFAULT (datetime('now')), vehicle_id INTEGER REFERENCES vehicles(id), paint_id INTEGER REFERENCES paints(id), from_vehicle_id INTEGER REFERENCES vehicles(id), to_vehicle_id INTEGER REFERENCES vehicles(id), buyback_type TEXT,
    FOREIGN KEY (user_id) REFERENCES user(id)
);
CREATE TABLE org_verification_pending (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL,
  rsi_sid TEXT NOT NULL,
  verification_key TEXT NOT NULL,
  created_at TEXT DEFAULT (datetime('now')),
  UNIQUE(rsi_sid)
);
CREATE TABLE org_join_codes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  organization_id TEXT NOT NULL,
  code TEXT NOT NULL UNIQUE,
  created_by TEXT NOT NULL,
  max_uses INTEGER,
  use_count INTEGER DEFAULT 0,
  expires_at TEXT,
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE TABLE user_localization_configs (
  id                         INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id                    TEXT    NOT NULL UNIQUE,
  asop_enabled               INTEGER NOT NULL DEFAULT 0,
  labels_vehicle_components  INTEGER NOT NULL DEFAULT 0,
  labels_fps_weapons         INTEGER NOT NULL DEFAULT 0,
  labels_fps_armour          INTEGER NOT NULL DEFAULT 0,
  labels_fps_helmets         INTEGER NOT NULL DEFAULT 0,
  labels_fps_attachments     INTEGER NOT NULL DEFAULT 0,
  labels_fps_utilities       INTEGER NOT NULL DEFAULT 0,
  labels_consumables         INTEGER NOT NULL DEFAULT 0,
  labels_ship_missiles       INTEGER NOT NULL DEFAULT 0,
  label_format               TEXT    NOT NULL DEFAULT 'suffix',
  updated_at                 TEXT    DEFAULT (datetime('now'))
, category_formats_json TEXT, enabled_packs_json TEXT, enhance_blueprint_pools INTEGER NOT NULL DEFAULT 0, enhance_contraband_warnings INTEGER NOT NULL DEFAULT 0, enhance_material_names INTEGER NOT NULL DEFAULT 0, enhance_contract_rep INTEGER NOT NULL DEFAULT 0, category_packs_json TEXT);
CREATE TABLE user_localization_ship_order (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id         TEXT    NOT NULL,
  vehicle_id      INTEGER NOT NULL REFERENCES vehicles(id),
  sort_position   INTEGER NOT NULL,
  custom_label    TEXT,
  UNIQUE(user_id, vehicle_id),
  UNIQUE(user_id, sort_position)
);
CREATE TABLE crafting_resources (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, [key] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE crafting_properties (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    key TEXT NOT NULL UNIQUE,
    name TEXT NOT NULL,
    unit TEXT,
    category TEXT NOT NULL
);
CREATE TABLE crafting_blueprints (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    tag TEXT NOT NULL,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    sub_type TEXT NOT NULL,
    product_entity_class TEXT,
    craft_time_seconds INTEGER NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, is_default INTEGER NOT NULL DEFAULT 0, category_id INTEGER REFERENCES crafting_blueprint_categories(id), [description] TEXT, [output_item] TEXT, [slug] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE crafting_blueprint_slots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    crafting_blueprint_id INTEGER NOT NULL REFERENCES crafting_blueprints(id),
    slot_index INTEGER NOT NULL DEFAULT 0,
    name TEXT NOT NULL,
    resource_name TEXT NOT NULL,
    quantity REAL NOT NULL DEFAULT 0,
    min_quality INTEGER NOT NULL DEFAULT 0
, [blueprint_uuid] TEXT, [slot_name] TEXT, slot_type TEXT NOT NULL DEFAULT 'resource', item_class TEXT);
CREATE TABLE crafting_slot_modifiers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    crafting_blueprint_slot_id INTEGER NOT NULL REFERENCES crafting_blueprint_slots(id),
    crafting_property_id INTEGER NOT NULL REFERENCES crafting_properties(id),
    start_quality INTEGER NOT NULL DEFAULT 0,
    end_quality INTEGER NOT NULL DEFAULT 1000,
    modifier_at_start REAL NOT NULL DEFAULT 1.0,
    modifier_at_end REAL NOT NULL DEFAULT 1.0
, [blueprint_uuid] TEXT, [property_id] TEXT, [slot_index] INTEGER);
CREATE TABLE mining_locations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    preset_file TEXT NOT NULL,
    preset_guid TEXT,
    name TEXT NOT NULL,
    system TEXT NOT NULL,
    location_type TEXT NOT NULL,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, [deposits] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE mining_location_deposits (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    mining_location_id INTEGER NOT NULL REFERENCES mining_locations(id),
    group_name TEXT NOT NULL,
    group_probability REAL NOT NULL DEFAULT 1.0,
    composition_guid TEXT NOT NULL,
    relative_probability REAL NOT NULL DEFAULT 1.0,
    clustering_preset_guid TEXT
, rock_composition_id INTEGER REFERENCES rock_compositions(id));
CREATE TABLE mining_clustering_params (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    mining_clustering_preset_id INTEGER NOT NULL REFERENCES mining_clustering_presets(id),
    relative_probability REAL NOT NULL DEFAULT 1.0,
    min_size INTEGER NOT NULL DEFAULT 1,
    max_size INTEGER NOT NULL DEFAULT 1,
    min_proximity REAL NOT NULL DEFAULT 0,
    max_proximity REAL NOT NULL DEFAULT 0
);
CREATE TABLE mining_quality_distributions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    min_quality INTEGER NOT NULL DEFAULT 1,
    max_quality INTEGER NOT NULL DEFAULT 1000,
    mean REAL NOT NULL DEFAULT 500,
    stddev REAL NOT NULL DEFAULT 250,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE mining_lasers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    size INTEGER NOT NULL DEFAULT 1,
    grade INTEGER NOT NULL DEFAULT 1,
    manufacturer TEXT,
    vehicle_built_in TEXT,
    module_slots INTEGER NOT NULL DEFAULT 0,
    throttle_lerp_speed REAL,
    throttle_minimum REAL,
    beam_full_range REAL,
    beam_zero_range REAL,
    beam_dps REAL,
    extract_full_range REAL,
    extract_zero_range REAL,
    extract_dps REAL,
    mod_instability REAL NOT NULL DEFAULT 0,
    mod_optimal_window_size REAL NOT NULL DEFAULT 0,
    mod_resistance REAL NOT NULL DEFAULT 0,
    mod_shatter_damage REAL NOT NULL DEFAULT 0,
    mod_cluster_factor REAL NOT NULL DEFAULT 0,
    mod_optimal_charge_rate REAL NOT NULL DEFAULT 0,
    mod_catastrophic_charge_rate REAL NOT NULL DEFAULT 0,
    mod_filter REAL NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE mining_modules (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    size INTEGER NOT NULL DEFAULT 1,
    grade INTEGER NOT NULL DEFAULT 1,
    manufacturer TEXT,
    charges INTEGER,
    lifetime REAL,
    damage_multiplier REAL NOT NULL DEFAULT 1.0,
    mod_instability REAL NOT NULL DEFAULT 0,
    mod_optimal_window_size REAL NOT NULL DEFAULT 0,
    mod_resistance REAL NOT NULL DEFAULT 0,
    mod_shatter_damage REAL NOT NULL DEFAULT 0,
    mod_cluster_factor REAL NOT NULL DEFAULT 0,
    mod_optimal_charge_rate REAL NOT NULL DEFAULT 0,
    mod_catastrophic_charge_rate REAL NOT NULL DEFAULT 0,
    mod_filter REAL NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE mining_gadgets (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    manufacturer TEXT,
    mod_instability REAL NOT NULL DEFAULT 0,
    mod_optimal_window_size REAL NOT NULL DEFAULT 0,
    mod_resistance REAL NOT NULL DEFAULT 0,
    mod_shatter_damage REAL NOT NULL DEFAULT 0,
    mod_cluster_factor REAL NOT NULL DEFAULT 0,
    mod_optimal_charge_rate REAL NOT NULL DEFAULT 0,
    mod_catastrophic_charge_rate REAL NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE salvageable_ships (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    entity_name TEXT NOT NULL,
    entity_uuid TEXT NOT NULL,
    base_vehicle_id INTEGER REFERENCES vehicles(id),
    variant_type TEXT NOT NULL,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, [base_class_name] TEXT, [uuid] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE salvageable_ship_components (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    salvageable_ship_id INTEGER NOT NULL REFERENCES salvageable_ships(id),
    port_name TEXT NOT NULL,
    port_type TEXT,
    component_uuid TEXT,
    component_name TEXT,
    component_type TEXT,
    component_sub_type TEXT,
    size INTEGER
);
CREATE TABLE profile_verification_pending (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL,
  handle TEXT NOT NULL,
  verification_key TEXT NOT NULL,
  created_at TEXT DEFAULT (datetime('now')), updated_at TEXT,
  UNIQUE(user_id)
);
CREATE TABLE op_types (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT NOT NULL UNIQUE,
  label TEXT NOT NULL
);
CREATE TABLE org_ops (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  org_id TEXT NOT NULL,
  name TEXT NOT NULL,
  op_type_id INTEGER NOT NULL REFERENCES op_types(id),
  status TEXT NOT NULL DEFAULT 'planning',
  description TEXT,
  created_by TEXT NOT NULL,
  started_at TEXT,
  completed_at TEXT,
  webhook_url TEXT,
  is_public INTEGER NOT NULL DEFAULT 0,
  join_code TEXT,
  join_code_expires_at TEXT,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
);
CREATE TABLE org_op_participants (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  org_op_id INTEGER NOT NULL REFERENCES org_ops(id),
  user_id TEXT NOT NULL,
  role TEXT NOT NULL DEFAULT 'member',
  payout_ratio REAL,
  joined_at TEXT DEFAULT (datetime('now')),
  left_at TEXT,
  logged_off INTEGER NOT NULL DEFAULT 0,
  UNIQUE(org_op_id, user_id)
);
CREATE TABLE org_op_ships (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  org_op_id INTEGER NOT NULL REFERENCES org_ops(id),
  user_fleet_id INTEGER NOT NULL REFERENCES user_fleet(id),
  owner_user_id TEXT NOT NULL,
  role TEXT,
  UNIQUE(org_op_id, user_fleet_id)
);
CREATE TABLE org_op_capital (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  org_op_id INTEGER NOT NULL REFERENCES org_ops(id),
  user_id TEXT NOT NULL,
  amount INTEGER NOT NULL DEFAULT 0,
  currency TEXT NOT NULL DEFAULT 'aUEC',
  note TEXT,
  paid INTEGER NOT NULL DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE TABLE org_op_earnings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  org_op_id INTEGER NOT NULL REFERENCES org_ops(id),
  amount INTEGER NOT NULL DEFAULT 0,
  currency TEXT NOT NULL DEFAULT 'aUEC',
  note TEXT,
  logged_by TEXT NOT NULL,
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE TABLE org_op_payouts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  org_op_id INTEGER NOT NULL REFERENCES org_ops(id),
  user_id TEXT NOT NULL,
  amount INTEGER NOT NULL DEFAULT 0,
  currency TEXT NOT NULL DEFAULT 'aUEC',
  paid INTEGER NOT NULL DEFAULT 0,
  paid_at TEXT,
  UNIQUE(org_op_id, user_id)
);
CREATE TABLE rating_categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT NOT NULL UNIQUE,
  label TEXT NOT NULL,
  description TEXT
);
CREATE TABLE player_ratings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  rater_user_id TEXT NOT NULL,
  ratee_user_id TEXT NOT NULL,
  org_op_id INTEGER REFERENCES org_ops(id),
  rating_category_id INTEGER NOT NULL REFERENCES rating_categories(id),
  score INTEGER NOT NULL CHECK (score >= 1 AND score <= 5),
  ip_address TEXT,
  created_at TEXT DEFAULT (datetime('now')),
  UNIQUE(rater_user_id, ratee_user_id, org_op_id, rating_category_id)
);
CREATE TABLE player_reviews (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  rater_user_id TEXT NOT NULL,
  ratee_user_id TEXT NOT NULL,
  org_op_id INTEGER REFERENCES org_ops(id),
  comment TEXT NOT NULL,
  ip_address TEXT,
  created_at TEXT DEFAULT (datetime('now')),
  UNIQUE(rater_user_id, ratee_user_id, org_op_id)
);
CREATE TABLE rating_audit_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  action TEXT NOT NULL,
  actor_user_id TEXT NOT NULL,
  target_rating_id INTEGER,
  target_review_id INTEGER,
  detail TEXT,
  ip_address TEXT,
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE TABLE player_reputation (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL,
  rating_category_id INTEGER NOT NULL REFERENCES rating_categories(id),
  median_score REAL NOT NULL DEFAULT 0,
  rating_count INTEGER NOT NULL DEFAULT 0,
  updated_at TEXT DEFAULT (datetime('now')),
  UNIQUE(user_id, rating_category_id)
);
CREATE TABLE companion_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    type            TEXT    NOT NULL,
    source          TEXT    NOT NULL DEFAULT 'log',
    event_timestamp TEXT    NOT NULL,
    data_json       TEXT    NOT NULL DEFAULT '{}',
    received_at     TEXT    NOT NULL DEFAULT (datetime('now'))
);
CREATE TABLE companion_status (
    user_id           TEXT PRIMARY KEY,
    player_handle     TEXT,
    current_ship      TEXT,
    location          TEXT,
    jurisdiction      TEXT,
    event_count       INTEGER NOT NULL DEFAULT 0,
    companion_version TEXT,
    updated_at        TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE TABLE user_fleet_loadout (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id          TEXT    NOT NULL,
  user_fleet_id    INTEGER NOT NULL REFERENCES user_fleet(id) ON DELETE CASCADE,
  port_id          INTEGER NOT NULL REFERENCES vehicle_ports(id),
  component_id     INTEGER NOT NULL REFERENCES vehicle_components(id),
  created_at       TEXT    DEFAULT (datetime('now')),
  updated_at       TEXT    DEFAULT (datetime('now')),
  UNIQUE(user_id, user_fleet_id, port_id)
);
CREATE TABLE user_loadout_cart (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id          TEXT    NOT NULL,
  component_id     INTEGER NOT NULL REFERENCES vehicle_components(id),
  shop_id          INTEGER REFERENCES shops(id),
  quantity         INTEGER NOT NULL DEFAULT 1,
  source_fleet_id  INTEGER REFERENCES user_fleet(id) ON DELETE SET NULL,
  created_at       TEXT    DEFAULT (datetime('now')),
  UNIQUE(user_id, component_id, source_fleet_id)
);
CREATE TABLE crafting_blueprint_reward_pool_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    crafting_blueprint_reward_pool_id INTEGER NOT NULL REFERENCES crafting_blueprint_reward_pools(id),
    crafting_blueprint_id INTEGER NOT NULL REFERENCES crafting_blueprints(id),
    weight INTEGER NOT NULL DEFAULT 1
, [blueprint_uuid] TEXT, [pool_key] TEXT);
CREATE TABLE user_blueprints (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL REFERENCES user(id),
    crafting_blueprint_id INTEGER REFERENCES crafting_blueprints(id),
    source TEXT,
    synced_at TEXT DEFAULT (datetime('now')), nickname TEXT, crafted_quantity INTEGER DEFAULT 0, quality_config_json TEXT, updated_at TEXT DEFAULT (datetime('now')), blueprint_uuid TEXT, is_owned INTEGER NOT NULL DEFAULT 1, is_wishlist INTEGER NOT NULL DEFAULT 0,
    UNIQUE(user_id, crafting_blueprint_id)
);
CREATE TABLE contract_blueprint_reward_pools (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    contract_generator_key TEXT NOT NULL,
    crafting_blueprint_reward_pool_id INTEGER NOT NULL REFERENCES crafting_blueprint_reward_pools(id),
    chance REAL NOT NULL DEFAULT 1.0,
    game_version_id INTEGER REFERENCES game_versions(id)
, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE image_captures (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  url         TEXT    NOT NULL,
  source      TEXT    NOT NULL DEFAULT 'hangar_sync',
  vehicle_id  INTEGER REFERENCES vehicles(id),
  vehicle_slug TEXT,
  title       TEXT,
  first_seen  TEXT    NOT NULL DEFAULT (datetime('now')),
  last_seen   TEXT    NOT NULL DEFAULT (datetime('now')),
  seen_count  INTEGER NOT NULL DEFAULT 1,
  promoted    INTEGER NOT NULL DEFAULT 0, kind TEXT, title_norm TEXT NOT NULL DEFAULT '', manual_paint_id INTEGER REFERENCES paints(id), manual_match_kind TEXT, manual_match_id INTEGER,
  UNIQUE(url)
);
CREATE TABLE companion_sessions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    player_handle   TEXT,
    shard           TEXT,
    started_at      TEXT    NOT NULL,
    ended_at        TEXT,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE companion_ship_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    event_type      TEXT    NOT NULL,
    ship_name       TEXT,
    owner_handle    TEXT,
    request_id      TEXT,              -- insurance claim request ID
    reason          TEXT,              -- impound reason
    ship_count      INTEGER,           -- entitlement/vehicle list count
    details         TEXT,              -- reconciliation details
    status          TEXT,              -- reconciliation status
    phase           TEXT,              -- reconciliation phase
    event_at        TEXT    NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE companion_mission_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    event_type      TEXT    NOT NULL,
    mission_name    TEXT,
    mission_id      TEXT,
    description     TEXT,              -- objective description
    player_handle   TEXT,
    completion_type TEXT,              -- from end_mission
    reason          TEXT,              -- from end_mission
    state           TEXT,              -- from mission_ended
    event_at        TEXT    NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE companion_location_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    event_type      TEXT    NOT NULL,
    location        TEXT,
    jurisdiction    TEXT,
    player_handle   TEXT,
    event_at        TEXT    NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE companion_travel_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    event_type      TEXT    NOT NULL,
    destination     TEXT,
    event_at        TEXT    NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE companion_economy_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    event_type      TEXT    NOT NULL,
    amount          INTEGER,           -- aUEC/UEC amount
    currency        TEXT,              -- UEC, aUEC
    recipient       TEXT,              -- money_sent target
    location        TEXT,              -- refinery location
    item_name       TEXT,              -- blueprint name
    reward_count    INTEGER,           -- rewards_earned count
    event_at        TEXT    NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE companion_combat_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    event_type      TEXT    NOT NULL,
    severity        TEXT,              -- injury severity (Minor/Major/Severe)
    body_part       TEXT,              -- injury body part
    tier            INTEGER,           -- injury tier
    vehicle         TEXT,              -- fatal_collision vehicle / med_bed vehicle
    zone            TEXT,              -- fatal_collision zone
    actor           TEXT,              -- actor_death / med_bed actor name
    bed_name        TEXT,              -- med_bed name
    crime           TEXT,              -- crime_committed crime type
    heal_head       INTEGER DEFAULT 0, -- med_bed body part flags
    heal_torso      INTEGER DEFAULT 0,
    heal_left_arm   INTEGER DEFAULT 0,
    heal_right_arm  INTEGER DEFAULT 0,
    heal_left_leg   INTEGER DEFAULT 0,
    heal_right_leg  INTEGER DEFAULT 0,
    event_at        TEXT    NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE companion_social_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    event_type      TEXT    NOT NULL,
    player_name     TEXT,              -- who joined/left
    event_at        TEXT    NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE companion_system_events (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         TEXT    NOT NULL,
    event_type      TEXT    NOT NULL,
    entry           TEXT,              -- journal entry text
    event_at        TEXT    NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE
);
CREATE TABLE contract_generator_careers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  contract_generator_id INTEGER NOT NULL REFERENCES contract_generators(id),
  debug_name TEXT NOT NULL,
  system TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id)
, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE contract_generator_blueprint_pools (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  contract_generator_contract_id INTEGER NOT NULL REFERENCES contract_generator_contracts(id),
  crafting_blueprint_reward_pool_id INTEGER NOT NULL REFERENCES crafting_blueprint_reward_pools(id),
  chance REAL DEFAULT 1.0,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id)
, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE user_paints (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id    TEXT    NOT NULL REFERENCES "user"(id) ON DELETE CASCADE,
    paint_id   INTEGER NOT NULL REFERENCES paints(id),
    pledge_id  TEXT,
    pledge_name TEXT,
    is_buyback INTEGER NOT NULL DEFAULT 0,
    synced_at  TEXT    NOT NULL DEFAULT (datetime('now')),
    UNIQUE(user_id, paint_id)
);
CREATE TABLE localization_overlay_packs (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  name         TEXT    NOT NULL UNIQUE,
  label        TEXT    NOT NULL,
  description  TEXT,
  icon         TEXT,
  sort_order   INTEGER NOT NULL DEFAULT 0,
  is_active    INTEGER NOT NULL DEFAULT 1,
  version_code TEXT,
  key_count    INTEGER NOT NULL DEFAULT 0,
  created_at   TEXT    DEFAULT (datetime('now')),
  updated_at   TEXT    DEFAULT (datetime('now'))
);
CREATE TABLE crafting_blueprint_categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid TEXT NOT NULL UNIQUE,
  name TEXT NOT NULL,
  slug TEXT NOT NULL,
  game_version TEXT,
  data_source TEXT,
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE TABLE terminals (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid TEXT NOT NULL UNIQUE,
  shop_id INTEGER REFERENCES shops(id),
  shop_name_key TEXT NOT NULL UNIQUE,
  terminal_type TEXT,
  uex_terminal_id INTEGER,
  game_version_id INTEGER REFERENCES game_versions(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE price_observations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  terminal_id INTEGER NOT NULL REFERENCES terminals(id),
  item_uuid TEXT NOT NULL,
  observation_type TEXT NOT NULL,
  price REAL NOT NULL,
  quantity INTEGER,
  price_per_unit REAL,
  source TEXT NOT NULL,
  user_id TEXT,
  observed_at TEXT NOT NULL,
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE TABLE unlinked_sources (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  source TEXT NOT NULL,
  source_identifier TEXT NOT NULL,
  raw_data TEXT,
  resolved INTEGER DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now'))
);
CREATE TABLE armor_resistance_profiles (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  physical          REAL,
  energy            REAL,
  distortion        REAL,
  thermal           REAL,
  biochemical       REAL,
  stun              REAL,
  impact_force      REAL,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE component_coolers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  cooling_rate REAL,
  max_temperature REAL,
  overheat_temperature REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE component_mining (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  mining_throttle_speed REAL,
  min_calibration_requirement REAL,
  max_calibration_requirement REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE component_powerplants (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  power_output REAL,
  overpower_performance REAL,
  overclock_performance REAL,
  overclock_threshold_min REAL,
  overclock_threshold_max REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, max_temperature REAL, overheat_temperature REAL,
  UNIQUE(component_id)
);
CREATE TABLE component_qed (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  qed_range REAL,
  qed_strength REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE component_quantum_drives (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  quantum_speed REAL,
  quantum_range REAL,
  fuel_rate REAL,
  spool_time REAL,
  cooldown_time REAL,
  stage1_accel REAL,
  stage2_accel REAL,
  calibration_rate REAL,
  engage_speed REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE component_radar (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  radar_range REAL,
  radar_angle REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE component_shields (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  shield_hp REAL,
  shield_regen REAL,
  resist_physical REAL,
  resist_energy REAL,
  resist_distortion REAL,
  resist_thermal REAL,
  regen_delay REAL,
  downed_regen_delay REAL,
  resist_physical_min REAL,
  resist_energy_min REAL,
  resist_distortion_min REAL,
  resist_thermal_min REAL,
  absorb_physical_min REAL,
  absorb_physical_max REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE component_thrusters (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  thrust_force REAL,
  fuel_burn_rate REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE component_turrets (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  rotation_speed REAL,
  min_pitch REAL,
  max_pitch REAL,
  min_yaw REAL,
  max_yaw REAL,
  gimbal_type TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE component_weapons (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  rounds_per_minute REAL,
  ammo_container_size INTEGER,
  damage_per_shot REAL,
  damage_type TEXT,
  projectile_speed REAL,
  effective_range REAL,
  dps REAL,
  heat_per_shot REAL,
  fire_modes TEXT,
  damage_physical REAL,
  damage_energy REAL,
  damage_distortion REAL,
  damage_thermal REAL,
  penetration REAL,
  weapon_range REAL,
  laser_instability REAL,
  optimal_charge_window_size REAL,
  resistance_modifier REAL,
  shatter_damage_modifier REAL,
  optimal_charge_rate REAL,
  catastrophic_charge_rate REAL,
  filter_modifier REAL,
  charge_time REAL,
  decay_ratio REAL,
  interdiction_effect_time REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE "consumable_effect_types" (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  key         TEXT    NOT NULL,
  name        TEXT    NOT NULL,
  description TEXT,
  is_positive INTEGER DEFAULT 1,
  game_version_id INTEGER REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(key)
);
CREATE TABLE consumable_effects (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  consumable_uuid TEXT NOT NULL,
  effect_key TEXT NOT NULL,
  magnitude REAL,
  duration_seconds REAL,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(consumable_uuid, effect_key)
);
CREATE TABLE consumables (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  type            TEXT,
  sub_type        TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [size] INTEGER, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE contract_generator_contracts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  career_id INTEGER NOT NULL REFERENCES contract_generator_careers(id),
  uuid TEXT NOT NULL,
  debug_name TEXT NOT NULL,
  difficulty TEXT,
  template TEXT,
  min_standing TEXT,
  max_standing TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), rep_reward INTEGER, rep_rewards_json TEXT, title_loc_key TEXT, desc_loc_key TEXT, item_rewards_json TEXT, completion_tags_json TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE contract_generators (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  generator_key TEXT NOT NULL,
  display_name TEXT,
  faction_name TEXT,
  guild TEXT,
  mission_type TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), description TEXT, focus TEXT, headquarters TEXT, leadership TEXT, faction_slug TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(generator_key)
);
CREATE TABLE contracts (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  contract_key     TEXT    NOT NULL,
  giver            TEXT    NOT NULL,
  giver_slug       TEXT    NOT NULL,
  category         TEXT    NOT NULL,
  sequence_num     INTEGER,
  title            TEXT    NOT NULL,
  description      TEXT,
  reward_text      TEXT,
  reward_amount    INTEGER DEFAULT 0,
  reward_currency  TEXT,
  is_dynamic_reward INTEGER DEFAULT 0,
  is_active        INTEGER DEFAULT 1,
  notes            TEXT,
  game_version_id  INTEGER NOT NULL REFERENCES game_versions(id), requirements_json TEXT, removed INTEGER NOT NULL DEFAULT 0, reward_vehicle_slug TEXT, [name] TEXT, [slug] TEXT, [uuid] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(contract_key)
);
CREATE TABLE crafting_blueprint_reward_pools (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    key TEXT NOT NULL,
    name TEXT,
    game_version_id INTEGER REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
    UNIQUE(key)
);
CREATE TABLE damage_types (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE faction_reputation_scopes (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  faction_id        INTEGER NOT NULL REFERENCES factions(id),
  reputation_scope_id INTEGER NOT NULL REFERENCES reputation_scopes(id),
  is_primary        INTEGER DEFAULT 0,
  source            TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(faction_id, reputation_scope_id)
);
CREATE TABLE factions (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  faction_type      TEXT,
  headquarters      TEXT,
  founded           TEXT,
  leadership        TEXT,
  area              TEXT,
  focus             TEXT,
  default_reaction  TEXT,
  can_arrest        INTEGER DEFAULT 0,
  polices_crime     INTEGER DEFAULT 0,
  no_legal_rights   INTEGER DEFAULT 0,
  allies_json       TEXT,
  enemies_json      TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE fps_ammo_types (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid               TEXT NOT NULL,
  name               TEXT NOT NULL,
  display_name       TEXT,
  slug               TEXT,
  class_name         TEXT,
  caliber            TEXT,
  damage_per_round   REAL,
  damage_type        TEXT,
  projectile_speed   REAL,
  magazine_capacity  INTEGER,
  game_version_id    INTEGER NOT NULL REFERENCES game_versions(id),
  created_at         TEXT DEFAULT (datetime('now')), manufacturer_id INTEGER REFERENCES manufacturers(id), removed INTEGER NOT NULL DEFAULT 0, [ammo_type] TEXT, [damage_distortion] REAL, [damage_energy] REAL, [damage_physical] REAL, [speed] REAL, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, damage_thermal REAL, damage_biochemical REAL, damage_stun REAL,
  UNIQUE(uuid)
);
CREATE TABLE fps_armour (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  class_name      TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  sub_type        TEXT,
  size            INTEGER,
  grade           TEXT,
  description     TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data        TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, resist_physical REAL, resist_energy REAL, resist_distortion REAL, resist_thermal REAL, resist_biochemical REAL, resist_stun REAL, ir_emission REAL, em_emission REAL, item_port_count INTEGER, removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [radiation_capacity] REAL, [radiation_dissipation] REAL, [temperature_max] REAL, [temperature_min] REAL, data_source TEXT, protected_body_parts TEXT, armor_weight TEXT, integrity_threshold REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE fps_attachments (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  class_name      TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  sub_type        TEXT,
  size            INTEGER,
  description     TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data        TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, zoom_scale REAL, second_zoom_scale REAL, damage_multiplier REAL, sound_radius_multiplier REAL, scope_type TEXT, zeroing_range REAL, zeroing_increment REAL, auto_zeroing_time REAL, is_low_light INTEGER DEFAULT 0, recoil_decay REAL, recoil_strength REAL, recoil_randomness REAL, spread_min REAL, spread_max REAL, spread_attack_time REAL, spread_decay_time REAL, light_type TEXT, light_color TEXT, light_intensity REAL, light_radius REAL, light_fov REAL, removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [magazine_capacity] INTEGER, [magazine_restock_count] INTEGER, [manufacturer_code] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, fire_rate_multiplier REAL, projectile_speed_multiplier REAL, heat_generation_multiplier REAL,
  UNIQUE(uuid)
);
CREATE TABLE fps_carryables (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT NOT NULL,
  name              TEXT NOT NULL,
  display_name      TEXT,
  slug              TEXT,
  class_name        TEXT,
  sub_type          TEXT,
  mass              REAL,
  interaction_type  TEXT,
  value             REAL,
  description       TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [size] INTEGER, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE fps_clothing (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  slot            TEXT,
  size            REAL,
  grade           TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), resist_physical REAL, resist_energy REAL, resist_distortion REAL, resist_thermal REAL, resist_biochemical REAL, resist_stun REAL, ir_emission REAL, em_emission REAL, storage_capacity INTEGER, temperature_range_min REAL, temperature_range_max REAL, removed INTEGER NOT NULL DEFAULT 0, sub_type TEXT, [can_loot] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [item_port_count] INTEGER, [loot_rarity] TEXT, [manufacturer_code] TEXT, [radiation_capacity] REAL, [radiation_dissipation] REAL, [temperature_max] REAL, [temperature_min] REAL, data_source TEXT, protected_body_parts TEXT, armor_weight TEXT, integrity_threshold REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE fps_helmets (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  sub_type        TEXT,
  size            REAL,
  grade           TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), resist_physical REAL, resist_energy REAL, resist_distortion REAL, resist_thermal REAL, resist_biochemical REAL, resist_stun REAL, ir_emission REAL, em_emission REAL, item_port_count INTEGER, atmosphere_capacity REAL, removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [has_visor_display] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [max_fov] REAL, [min_fov] REAL, [radiation_capacity] REAL, [radiation_dissipation] REAL, [temperature_max] REAL, [temperature_min] REAL, data_source TEXT, protected_body_parts TEXT, armor_weight TEXT, integrity_threshold REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE fps_melee (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT NOT NULL,
  name              TEXT NOT NULL,
  display_name      TEXT,
  slug              TEXT,
  class_name        TEXT,
  manufacturer_id   INTEGER REFERENCES manufacturers(id),
  sub_type          TEXT,
  size              INTEGER,
  description       TEXT,
  damage            REAL,
  damage_type       TEXT,
  attack_speed      REAL,
  range             REAL,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT DEFAULT (datetime('now')),
  updated_at        TEXT DEFAULT (datetime('now')), heavy_damage REAL, attack_types REAL, can_block INTEGER, can_takedown INTEGER, removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE fps_utilities (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  class_name      TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  sub_type        TEXT,
  description     TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data        TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, heal_amount REAL, effect_duration REAL, consumable_type TEXT, damage REAL, blast_radius REAL, fuse_time REAL, device_type TEXT, removed INTEGER NOT NULL DEFAULT 0, detonation_type TEXT, [can_loot] INTEGER, [grade] TEXT, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [size] INTEGER, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE fps_weapons (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  class_name      TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  sub_type        TEXT,
  size            INTEGER,
  description     TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data        TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, rounds_per_minute REAL, fire_modes TEXT, burst_count INTEGER, ammo_capacity INTEGER, zoom_factor REAL, item_port_count INTEGER, damage REAL, damage_type TEXT, projectile_speed REAL, effective_range REAL, dps REAL, magazine_uuid TEXT, spread_min REAL, spread_max REAL, removed INTEGER NOT NULL DEFAULT 0, heat_per_shot REAL, charge_time REAL, recoil_strength REAL, weapon_class TEXT, [can_loot] INTEGER, [cooling_per_second] REAL, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [heal_range] REAL, [heal_rate] REAL, [heal_sensor_range] REAL, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [mining_throttle_speed] REAL, [overheat_fix_time] REAL, [overheat_temperature] REAL, [projectile_lifetime] REAL, [time_to_cooling_starts] REAL, [tool_type] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, has_builtin_scope INTEGER NOT NULL DEFAULT 0,
  UNIQUE(uuid)
);
CREATE TABLE harvestables (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  sub_type        TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE jurisdiction_infraction_overrides (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  jurisdiction_id     INTEGER NOT NULL REFERENCES law_jurisdictions(id),
  infraction_id       INTEGER NOT NULL REFERENCES law_infractions(id),
  overrides_json      TEXT,
  game_version_id     INTEGER NOT NULL REFERENCES game_versions(id),
  created_at          TEXT    DEFAULT (datetime('now')),
  updated_at          TEXT    DEFAULT (datetime('now')), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(jurisdiction_id, infraction_id)
);
CREATE TABLE law_infractions (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  severity          TEXT    NOT NULL,
  triggers_json     TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), is_felony INTEGER, grace_allowance REAL, grace_allowance_cooldown REAL, grace_period REAL, grace_cooloff_scale REAL, display_grace_time INTEGER, escalated_fine_multiplier REAL, early_payment_period REAL, lifetime REAL, cool_off_time REAL, press_charges_notification_time REAL, remove_time_seconds REAL, felony_merits REAL, ignore_party_member INTEGER, hide_crime_notification INTEGER, hide_crime_journal INTEGER, removed INTEGER NOT NULL DEFAULT 0, [cool_off_seconds] REAL, [escalation_multiplier] REAL, [fine_amount] REAL, [lifetime_hours] REAL, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE law_jurisdictions (
  id                    INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                  TEXT    NOT NULL,
  name                  TEXT    NOT NULL,
  slug                  TEXT,
  parent_uuid           TEXT,
  respects_parent_laws  INTEGER DEFAULT 1,
  base_fine             INTEGER,
  is_prison             INTEGER DEFAULT 0,
  max_stolen_goods_scu  REAL,
  prohibited_goods_json TEXT,
  controlled_substances_json TEXT,
  game_version_id       INTEGER NOT NULL REFERENCES game_versions(id),
  created_at            TEXT    DEFAULT (datetime('now')),
  updated_at            TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE loot_map (
  id                   INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                 TEXT    NOT NULL,
  name                 TEXT    NOT NULL,
  class_name           TEXT,
  type                 TEXT,
  sub_type             TEXT,
  rarity               TEXT,
  vehicle_component_id INTEGER REFERENCES vehicle_components(id),
  fps_weapon_id        INTEGER REFERENCES fps_weapons(id),
  fps_armour_id        INTEGER REFERENCES fps_armour(id),
  fps_attachment_id    INTEGER REFERENCES fps_attachments(id),
  fps_utility_id       INTEGER REFERENCES fps_utilities(id),
  fps_helmet_id        INTEGER REFERENCES fps_helmets(id),
  fps_clothing_id      INTEGER REFERENCES fps_clothing(id),
  consumable_id        INTEGER REFERENCES consumables(id),
  harvestable_id       INTEGER REFERENCES harvestables(id),
  props_id             INTEGER REFERENCES props(id),
  game_version_id      INTEGER NOT NULL REFERENCES game_versions(id),
  updated_at           TEXT    DEFAULT (datetime('now')), ship_missile_id INTEGER REFERENCES ship_missiles(id), manufacturer_name TEXT, category TEXT, removed INTEGER NOT NULL DEFAULT 0, fps_melee_id INTEGER REFERENCES fps_melee(id), fps_carryable_id INTEGER REFERENCES fps_carryables(id), [loot_rarity] TEXT, [slug] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE manufacturers (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT    NOT NULL,
  code            TEXT,
  known_for       TEXT,
  description     TEXT,
  logo_url        TEXT,
  raw_data        TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, class TEXT, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE "mining_clustering_presets" (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL,
    name TEXT NOT NULL,
    probability_of_clustering REAL NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, [params] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
    UNIQUE(uuid)
);
CREATE TABLE mission_givers (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  faction_id        INTEGER REFERENCES factions(id),
  location_id       INTEGER REFERENCES star_map_locations(id),
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, biography TEXT, occupation TEXT, association TEXT, headquarters TEXT, portrait_url TEXT, is_lawful INTEGER, allies_json TEXT, enemies_json TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE mission_organizations (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE mission_prerequisites (
  mission_id INTEGER NOT NULL REFERENCES missions(id),
  required_mission_id INTEGER NOT NULL REFERENCES missions(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  PRIMARY KEY (mission_id, required_mission_id)
);
CREATE TABLE mission_types (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  category          TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [icon_name] TEXT, [svg_icon_path] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE missions (
  id                    INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                  TEXT NOT NULL,
  title                 TEXT,
  display_name          TEXT,
  description           TEXT,
  mission_type_id       INTEGER REFERENCES mission_types(id),
  mission_giver_id      INTEGER REFERENCES mission_givers(id),
  reward_amount         INTEGER DEFAULT 0,
  reward_currency       TEXT DEFAULT 'aUEC',
  reputation_scope_id   INTEGER REFERENCES reputation_scopes(id),
  reputation_reward_size TEXT,
  min_reputation        INTEGER,
  is_lawful             INTEGER,
  not_for_release       INTEGER DEFAULT 0,
  location_hint         TEXT,
  category              TEXT,
  subcategory           TEXT,
  difficulty            TEXT,
  game_version_id       INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, rep_fail_summary TEXT, rep_abandon_summary TEXT, time_limit_minutes INTEGER, max_players INTEGER, can_share INTEGER DEFAULT 1, once_only INTEGER DEFAULT 0, fail_if_criminal INTEGER, available_in_prison INTEGER DEFAULT 0, wanted_level_min INTEGER DEFAULT 0, wanted_level_max INTEGER DEFAULT 5, buy_in_amount INTEGER DEFAULT 0, reward_max INTEGER, has_standing_bonus INTEGER DEFAULT 0, location_ref TEXT, locality TEXT, [mission_giver] TEXT, [mission_type] TEXT, [name] TEXT, [reward_min] INTEGER, [slug] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, is_dynamic_reward INTEGER NOT NULL DEFAULT 0,
  UNIQUE(uuid)
);
CREATE TABLE npc_loadouts (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  file_path         TEXT NOT NULL,
  loadout_name      TEXT NOT NULL,
  faction_id        INTEGER REFERENCES npc_factions(id),
  category          TEXT NOT NULL,
  sub_category      TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT DEFAULT (datetime('now')), visible_item_count INTEGER DEFAULT 0, removed INTEGER NOT NULL DEFAULT 0, [faction] TEXT, [item_count] INTEGER, [name] TEXT, [species] TEXT, [uuid] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(file_path)
);
CREATE TABLE "paints" (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT,
    name TEXT NOT NULL,
    slug TEXT,
    class_name TEXT,
    description TEXT,
    image_url TEXT,
    image_url_small TEXT,
    image_url_medium TEXT,
    image_url_large TEXT,
    raw_data TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_base_variant INTEGER DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id), [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [size] INTEGER, [sub_type] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, title_norm TEXT NOT NULL DEFAULT '',
    UNIQUE(class_name)
);
CREATE TABLE props (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  type            TEXT,
  sub_type        TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [manufacturer_code] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE refining_processes (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  speed             TEXT    NOT NULL,
  quality           TEXT    NOT NULL,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE reputation_perks (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  scope_id          INTEGER NOT NULL REFERENCES reputation_scopes(id),
  standing_id       INTEGER NOT NULL REFERENCES reputation_standings(id),
  perk_name         TEXT NOT NULL,
  display_name      TEXT,
  description       TEXT,
  reward_item_uuid  TEXT,
  reward_item_name  TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(scope_id, standing_id, perk_name)
);
CREATE TABLE reputation_reward_tiers (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  size_code       TEXT NOT NULL,
  direction       TEXT NOT NULL,
  rep_amount      INTEGER NOT NULL,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(size_code, direction)
);
CREATE TABLE reputation_scopes (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  scope_key         TEXT    NOT NULL,
  description       TEXT,
  faction_id        INTEGER REFERENCES factions(id),
  max_reputation    INTEGER,
  initial_reputation INTEGER DEFAULT 0,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE "reputation_standings" (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  scope_id          INTEGER NOT NULL REFERENCES reputation_scopes(id),
  min_reputation    INTEGER NOT NULL,
  drift_reputation  INTEGER DEFAULT 0,
  drift_time_hours  REAL    DEFAULT 0,
  is_gated          INTEGER DEFAULT 0,
  perk_description  TEXT,
  sort_order        INTEGER,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [description] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid, scope_id)
);
CREATE TABLE rock_compositions (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  class_name        TEXT,
  rock_type         TEXT,
  min_elements      INTEGER,
  composition_json  TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, deposit_name TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ship_missiles (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  type            TEXT,
  sub_type        TEXT,
  size            INTEGER,
  grade           TEXT,
  manufacturer_id INTEGER REFERENCES manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), missile_type TEXT, lock_time REAL, tracking_signal TEXT, damage REAL, damage_type TEXT, blast_radius REAL, speed REAL, lock_range REAL, ammo_count INTEGER, removed INTEGER NOT NULL DEFAULT 0, [manufacturer_code] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE "shop_franchises" (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  localization_key  TEXT,
  slug              TEXT,
  game_version_id   INTEGER REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE shop_locations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  shop_id INTEGER NOT NULL REFERENCES shops(id),
  location_id INTEGER NOT NULL REFERENCES star_map_locations(id),
  placement_name TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(shop_id, location_id)
);
CREATE TABLE shops (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  shop_type         TEXT,
  location_id       INTEGER REFERENCES star_map_locations(id),
  is_event          INTEGER DEFAULT 0,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), location_label TEXT, franchise_uuid TEXT, display_name TEXT, inventory_type TEXT, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, component_type TEXT, scshop_label TEXT,
  UNIQUE(uuid)
);
CREATE TABLE star_map_locations (
  id                    INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                  TEXT    NOT NULL,
  name                  TEXT    NOT NULL,
  slug                  TEXT,
  description           TEXT,
  location_type         TEXT    NOT NULL,
  parent_uuid           TEXT,
  star_system_id        INTEGER REFERENCES star_systems(id),
  jurisdiction_id       INTEGER REFERENCES law_jurisdictions(id),
  is_scannable          INTEGER DEFAULT 0,
  hide_in_starmap       INTEGER DEFAULT 0,
  game_version_id       INTEGER NOT NULL REFERENCES game_versions(id),
  created_at            TEXT    DEFAULT (datetime('now')),
  updated_at            TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [affiliation] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE star_systems (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  code              TEXT,
  description       TEXT,
  galactic_x        REAL,
  galactic_y        REAL,
  galactic_z        REAL,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE terminal_inventory (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  terminal_id INTEGER NOT NULL REFERENCES terminals(id),
  item_uuid TEXT NOT NULL,
  item_type TEXT,
  item_name TEXT,
  base_buy_price REAL,
  base_sell_price REAL,
  latest_buy_price REAL,
  latest_sell_price REAL,
  latest_source TEXT,
  latest_observed_at TEXT,
  base_inventory REAL,
  max_inventory REAL,
  game_version_id INTEGER REFERENCES game_versions(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now')), is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, uex_date_modified INTEGER, uex_date_added INTEGER,
  UNIQUE(terminal_id, item_uuid)
);
CREATE TABLE trade_commodities (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid            TEXT NOT NULL,
    name            TEXT NOT NULL,
    slug            TEXT NOT NULL,
    class_name      TEXT,
    category        TEXT,        -- Directory category: metals, minerals, gas, vice, food, etc.
    type_name       TEXT,        -- Commodity type: Metal, Gas, Mineral, etc.
    subtype_name    TEXT,        -- Commodity subtype: Agricium, Hydrogen, etc.
    is_raw          INTEGER DEFAULT 0,  -- 1 = unrefined/ore form
    boxable         INTEGER DEFAULT 0,  -- 1 = can be boxed as physical commodity
    scu_per_unit    REAL,        -- SCU occupancy per unit (from centiSCU / 100)
    description     TEXT,
    game_version_id INTEGER REFERENCES game_versions(id),
    created_at      TEXT DEFAULT (datetime('now')),
    updated_at      TEXT DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [sub_category] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
    UNIQUE(uuid)
);
CREATE TABLE vehicle_careers (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE "vehicle_components" (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid TEXT NOT NULL,
  name TEXT NOT NULL,
  slug TEXT,
  class_name TEXT,
  manufacturer_id INTEGER,
  type TEXT NOT NULL,
  sub_type TEXT,
  size INTEGER,
  grade TEXT,
  description TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  class TEXT,
  component_class TEXT,
  em_signature REAL,
  mass REAL,
  hp REAL,
  power_draw REAL,
  power_draw_min REAL,
  thermal_output REAL,
  base_heat_generation REAL,
  distortion_max REAL,
  removed INTEGER NOT NULL DEFAULT 0,
  manufacturer_code TEXT,
  can_loot INTEGER,
  loot_rarity TEXT,
  inventory_volume REAL,
  grid_width INTEGER,
  grid_height INTEGER,
  capacity REAL,
  data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE vehicle_modules (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  vehicle_id      INTEGER NOT NULL REFERENCES vehicles(id),
  port_name       TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  display_name    TEXT,
  size            INTEGER,
  tags            TEXT,
  is_default      INTEGER NOT NULL DEFAULT 0,
  has_loadout     INTEGER NOT NULL DEFAULT 0,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  removed         INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid, vehicle_id)
);
CREATE TABLE vehicle_ports (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid               TEXT    NOT NULL,
  vehicle_id         INTEGER NOT NULL REFERENCES vehicles(id),
  parent_port_id     INTEGER REFERENCES vehicle_ports(id),
  name               TEXT    NOT NULL,
  position           TEXT,
  category_label     TEXT,
  size_min           INTEGER,
  size_max           INTEGER,
  port_type          TEXT,
  port_subtype       TEXT,
  equipped_item_uuid TEXT,
  editable           BOOLEAN DEFAULT TRUE,
  health             REAL,
  game_version_id    INTEGER NOT NULL REFERENCES game_versions(id),
  created_at         DATETIME DEFAULT CURRENT_TIMESTAMP, controller TEXT, controller_label TEXT, missile_type TEXT, removed INTEGER NOT NULL DEFAULT 0, [max_size] INTEGER, [min_size] INTEGER, [parent_port_name] TEXT, [port_name] TEXT, [vehicle_name] TEXT, [vehicle_uuid] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE vehicle_roles (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE vehicle_suit_lockers (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  vehicle_id         INTEGER REFERENCES vehicles(id),
  locker_entity_name TEXT NOT NULL,
  uuid               TEXT NOT NULL,
  locker_label       TEXT,
  locker_count       INTEGER DEFAULT 1,
  game_version_id    INTEGER NOT NULL REFERENCES game_versions(id),
  removed            INTEGER NOT NULL DEFAULT 0,
  vehicle_name       TEXT,
  data_source        TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, entity_name TEXT, port_name TEXT, min_size INTEGER, max_size INTEGER,
  UNIQUE(uuid, vehicle_id)
);
CREATE TABLE vehicle_weapon_racks (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  vehicle_id        INTEGER REFERENCES vehicles(id),
  rack_entity_name  TEXT NOT NULL,
  uuid              TEXT NOT NULL,
  rack_label        TEXT,
  total_ports       INTEGER NOT NULL DEFAULT 0,
  rifle_ports       INTEGER NOT NULL DEFAULT 0,
  pistol_ports      INTEGER NOT NULL DEFAULT 0,
  heavy_ports       INTEGER NOT NULL DEFAULT 0,
  utility_ports     INTEGER NOT NULL DEFAULT 0,
  min_size          INTEGER,
  max_size          INTEGER,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  removed           INTEGER NOT NULL DEFAULT 0,
  vehicle_name      TEXT,
  entity_name       TEXT,
  port_name         TEXT,
  rack_type         TEXT,
  data_source       TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid, vehicle_id)
);
CREATE TABLE vehicles (
  id                        INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                      TEXT,
  slug                      TEXT    NOT NULL,
  name                      TEXT    NOT NULL,
  class_name                TEXT,
  manufacturer_id           INTEGER REFERENCES manufacturers(id),
  vehicle_type_id           INTEGER REFERENCES vehicle_types(id),
  production_status_id      INTEGER REFERENCES production_statuses(id),
  size                      INTEGER,
  size_label                TEXT,
  focus                     TEXT,
  classification            TEXT,
  description               TEXT,
  length                    REAL,
  beam                      REAL,
  height                    REAL,
  mass                      REAL,
  cargo                     REAL,
  vehicle_inventory         REAL,
  crew_min                  INTEGER,
  crew_max                  INTEGER,
  speed_scm                 REAL,
  speed_max                 REAL,
  hull_damage_normalization                    REAL,
  pledge_price              REAL,
  price_auec                REAL,
  on_sale                   BOOLEAN DEFAULT FALSE,
  image_url                 TEXT,
  image_url_small           TEXT,
  image_url_medium          TEXT,
  image_url_large           TEXT,
  pledge_url                TEXT,
  game_version_id           INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data                  TEXT,
  created_at                DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at                DATETIME DEFAULT CURRENT_TIMESTAMP,
  parent_vehicle_id         INTEGER REFERENCES vehicles(id),
  is_paint_variant          INTEGER NOT NULL DEFAULT 0,
  boost_speed_back          INTEGER,
  angular_velocity_pitch    REAL,
  angular_velocity_yaw      REAL,
  angular_velocity_roll     REAL,
  fuel_capacity_hydrogen    REAL,
  fuel_capacity_quantum     REAL,
  thruster_count_main       INTEGER,
  thruster_count_maneuvering INTEGER,
  acquisition_type          TEXT, acquisition_source_name TEXT, cross_section_x REAL, cross_section_y REAL, cross_section_z REAL, ir_signature REAL, em_signature REAL, removed INTEGER NOT NULL DEFAULT 0, armor_hp INTEGER DEFAULT 0, armor_damage_physical REAL DEFAULT 1, armor_damage_energy REAL DEFAULT 1, armor_damage_distortion REAL DEFAULT 1, armor_damage_thermal REAL DEFAULT 1, armor_deflection_physical REAL DEFAULT 0, armor_deflection_energy REAL DEFAULT 0, armor_signal_ir REAL DEFAULT 1, armor_signal_em REAL DEFAULT 1, armor_signal_cs REAL DEFAULT 1, weapon_pool_size INTEGER DEFAULT 0, shield_pool_max INTEGER DEFAULT 0, replaced_by_vehicle_id INTEGER REFERENCES vehicles(id), [manufacturer_code] TEXT, [vehicle_type] TEXT, data_source TEXT, claim_time REAL, expedited_claim_time REAL, expedited_claim_cost INTEGER, is_pledgeable INTEGER NOT NULL DEFAULT 1, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, short_slug TEXT, is_variant INTEGER NOT NULL DEFAULT 0, is_purchasable_ingame INTEGER NOT NULL DEFAULT 0, g_force_resistance REAL, allow_room_connection INTEGER DEFAULT 0, is_npc_only INTEGER NOT NULL DEFAULT 0, hull_hp REAL, internal_cargo_scu REAL, external_cargo_scu REAL, fuel_cargo_scu REAL, personal_grid_microscu REAL, locker_count INTEGER DEFAULT 0,
  UNIQUE(slug)
);
CREATE TABLE "vehicle_career_assignments" (
  vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
  career_id  INTEGER NOT NULL REFERENCES vehicle_careers(id),
  PRIMARY KEY (vehicle_id, career_id)
);
CREATE TABLE "vehicle_role_assignments" (
  vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
  role_id    INTEGER NOT NULL REFERENCES vehicle_roles(id),
  PRIMARY KEY (vehicle_id, role_id)
);
CREATE TABLE "component_missiles" (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  ammo_count INTEGER,
  missile_type TEXT,
  UNIQUE(component_id, game_version_id)
);
CREATE TABLE "mission_reputation_requirements" (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  mission_id INTEGER NOT NULL REFERENCES missions(id),
  faction_slug TEXT NOT NULL,
  scope_slug TEXT NOT NULL,
  comparison TEXT NOT NULL,
  standing_slug TEXT NOT NULL,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  data_source TEXT DEFAULT 'p4k',
  is_deleted INTEGER DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT,
  UNIQUE(mission_id, faction_slug, scope_slug, comparison, standing_slug, game_version_id)
);
CREATE TABLE user_characters (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id       TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
  name          TEXT    NOT NULL,
  chf_key       TEXT    NOT NULL,
  headshot_key  TEXT,
  file_size     INTEGER NOT NULL,
  created_at    TEXT    NOT NULL DEFAULT (datetime('now')),
  updated_at    TEXT    NOT NULL DEFAULT (datetime('now'))
);
CREATE TABLE ptu_vehicle_weapon_racks (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  vehicle_id        INTEGER REFERENCES ptu_vehicles(id),
  rack_entity_name  TEXT NOT NULL,
  uuid              TEXT NOT NULL,
  rack_label        TEXT,
  total_ports       INTEGER NOT NULL DEFAULT 0,
  rifle_ports       INTEGER NOT NULL DEFAULT 0,
  pistol_ports      INTEGER NOT NULL DEFAULT 0,
  heavy_ports       INTEGER NOT NULL DEFAULT 0,
  utility_ports     INTEGER NOT NULL DEFAULT 0,
  min_size          INTEGER,
  max_size          INTEGER,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  removed           INTEGER NOT NULL DEFAULT 0,
  vehicle_name      TEXT,
  entity_name       TEXT,
  port_name         TEXT,
  rack_type         TEXT,
  data_source       TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid, vehicle_id)
);
CREATE TABLE ptu_vehicle_suit_lockers (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  vehicle_id         INTEGER REFERENCES ptu_vehicles(id),
  locker_entity_name TEXT NOT NULL,
  uuid               TEXT NOT NULL,
  locker_label       TEXT,
  locker_count       INTEGER DEFAULT 1,
  game_version_id    INTEGER NOT NULL REFERENCES game_versions(id),
  removed            INTEGER NOT NULL DEFAULT 0,
  vehicle_name       TEXT,
  data_source        TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, entity_name TEXT, port_name TEXT, min_size INTEGER, max_size INTEGER,
  UNIQUE(uuid, vehicle_id)
);
CREATE TABLE ptu_vehicle_ports (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid               TEXT    NOT NULL,
  vehicle_id         INTEGER NOT NULL REFERENCES ptu_vehicles(id),
  parent_port_id     INTEGER REFERENCES ptu_vehicle_ports(id),
  name               TEXT    NOT NULL,
  position           TEXT,
  category_label     TEXT,
  size_min           INTEGER,
  size_max           INTEGER,
  port_type          TEXT,
  port_subtype       TEXT,
  equipped_item_uuid TEXT,
  editable           BOOLEAN DEFAULT TRUE,
  health             REAL,
  game_version_id    INTEGER NOT NULL REFERENCES game_versions(id),
  created_at         DATETIME DEFAULT CURRENT_TIMESTAMP, controller TEXT, controller_label TEXT, missile_type TEXT, removed INTEGER NOT NULL DEFAULT 0, [max_size] INTEGER, [min_size] INTEGER, [parent_port_name] TEXT, [port_name] TEXT, [vehicle_name] TEXT, [vehicle_uuid] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_vehicle_modules (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  vehicle_id      INTEGER NOT NULL REFERENCES ptu_vehicles(id),
  port_name       TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  display_name    TEXT,
  size            INTEGER,
  tags            TEXT,
  is_default      INTEGER NOT NULL DEFAULT 0,
  has_loadout     INTEGER NOT NULL DEFAULT 0,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  removed         INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid, vehicle_id)
);
CREATE TABLE ptu_vehicle_roles (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_vehicle_careers (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_salvageable_ships (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    entity_name TEXT NOT NULL,
    entity_uuid TEXT NOT NULL,
    base_vehicle_id INTEGER REFERENCES ptu_vehicles(id),
    variant_type TEXT NOT NULL,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, [base_class_name] TEXT, [uuid] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_loot_item_locations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  loot_map_id INTEGER NOT NULL REFERENCES ptu_loot_map(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  source_type TEXT NOT NULL CHECK(source_type IN ('container','shop','npc','contract')),
  location_key TEXT NOT NULL,
  
  container_type TEXT,
  per_container REAL,
  per_roll REAL,
  rolls INTEGER,
  loot_table TEXT,
  
  actor TEXT,
  faction TEXT,
  slot TEXT,
  probability REAL,
  
  weight REAL
, spawn_locations TEXT, [item_uuid] TEXT, [location_label] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_loot_map (
  id                   INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                 TEXT    NOT NULL,
  name                 TEXT    NOT NULL,
  class_name           TEXT,
  type                 TEXT,
  sub_type             TEXT,
  rarity               TEXT,
  vehicle_component_id INTEGER REFERENCES ptu_vehicle_components(id),
  fps_weapon_id        INTEGER REFERENCES ptu_fps_weapons(id),
  fps_armour_id        INTEGER REFERENCES ptu_fps_armour(id),
  fps_attachment_id    INTEGER REFERENCES ptu_fps_attachments(id),
  fps_utility_id       INTEGER REFERENCES ptu_fps_utilities(id),
  fps_helmet_id        INTEGER REFERENCES ptu_fps_helmets(id),
  fps_clothing_id      INTEGER REFERENCES ptu_fps_clothing(id),
  consumable_id        INTEGER REFERENCES ptu_consumables(id),
  harvestable_id       INTEGER REFERENCES ptu_harvestables(id),
  props_id             INTEGER REFERENCES ptu_props(id),
  game_version_id      INTEGER NOT NULL REFERENCES game_versions(id),
  updated_at           TEXT    DEFAULT (datetime('now')), ship_missile_id INTEGER REFERENCES ptu_ship_missiles(id), manufacturer_name TEXT, category TEXT, removed INTEGER NOT NULL DEFAULT 0, fps_melee_id INTEGER REFERENCES ptu_fps_melee(id), fps_carryable_id INTEGER REFERENCES ptu_fps_carryables(id), [loot_rarity] TEXT, [slug] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_vehicle_components (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid TEXT NOT NULL,
  name TEXT NOT NULL,
  slug TEXT,
  class_name TEXT,
  manufacturer_id INTEGER,
  type TEXT NOT NULL,
  sub_type TEXT,
  size INTEGER,
  grade TEXT,
  description TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  class TEXT,
  component_class TEXT,
  em_signature REAL,
  mass REAL,
  hp REAL,
  power_draw REAL,
  power_draw_min REAL,
  thermal_output REAL,
  base_heat_generation REAL,
  distortion_max REAL,
  removed INTEGER NOT NULL DEFAULT 0,
  manufacturer_code TEXT,
  can_loot INTEGER,
  loot_rarity TEXT,
  inventory_volume REAL,
  grid_width INTEGER,
  grid_height INTEGER,
  capacity REAL,
  data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_component_coolers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  cooling_rate REAL,
  max_temperature REAL,
  overheat_temperature REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_mining (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  mining_throttle_speed REAL,
  min_calibration_requirement REAL,
  max_calibration_requirement REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_missiles (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  ammo_count INTEGER,
  missile_type TEXT,
  UNIQUE(component_id, game_version_id)
);
CREATE TABLE ptu_component_powerplants (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  power_output REAL,
  overpower_performance REAL,
  overclock_performance REAL,
  overclock_threshold_min REAL,
  overclock_threshold_max REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, max_temperature REAL, overheat_temperature REAL,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_qed (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  qed_range REAL,
  qed_strength REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_quantum_drives (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  quantum_speed REAL,
  quantum_range REAL,
  fuel_rate REAL,
  spool_time REAL,
  cooldown_time REAL,
  stage1_accel REAL,
  stage2_accel REAL,
  calibration_rate REAL,
  engage_speed REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_radar (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  radar_range REAL,
  radar_angle REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_shields (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  shield_hp REAL,
  shield_regen REAL,
  resist_physical REAL,
  resist_energy REAL,
  resist_distortion REAL,
  resist_thermal REAL,
  regen_delay REAL,
  downed_regen_delay REAL,
  resist_physical_min REAL,
  resist_energy_min REAL,
  resist_distortion_min REAL,
  resist_thermal_min REAL,
  absorb_physical_min REAL,
  absorb_physical_max REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_thrusters (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  thrust_force REAL,
  fuel_burn_rate REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_turrets (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  rotation_speed REAL,
  min_pitch REAL,
  max_pitch REAL,
  min_yaw REAL,
  max_yaw REAL,
  gimbal_type TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_weapons (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  rounds_per_minute REAL,
  ammo_container_size INTEGER,
  damage_per_shot REAL,
  damage_type TEXT,
  projectile_speed REAL,
  effective_range REAL,
  dps REAL,
  heat_per_shot REAL,
  fire_modes TEXT,
  damage_physical REAL,
  damage_energy REAL,
  damage_distortion REAL,
  damage_thermal REAL,
  penetration REAL,
  weapon_range REAL,
  laser_instability REAL,
  optimal_charge_window_size REAL,
  resistance_modifier REAL,
  shatter_damage_modifier REAL,
  optimal_charge_rate REAL,
  catastrophic_charge_rate REAL,
  filter_modifier REAL,
  charge_time REAL,
  decay_ratio REAL,
  interdiction_effect_time REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_shop_locations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  shop_id INTEGER NOT NULL REFERENCES ptu_shops(id),
  location_id INTEGER NOT NULL REFERENCES ptu_star_map_locations(id),
  placement_name TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(shop_id, location_id)
);
CREATE TABLE ptu_shop_franchises (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  localization_key  TEXT,
  slug              TEXT,
  game_version_id   INTEGER REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_terminals (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid TEXT NOT NULL UNIQUE,
  shop_id INTEGER REFERENCES ptu_shops(id),
  shop_name_key TEXT NOT NULL UNIQUE,
  terminal_type TEXT,
  uex_terminal_id INTEGER,
  game_version_id INTEGER REFERENCES game_versions(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_mission_prerequisites (
  mission_id INTEGER NOT NULL REFERENCES ptu_missions(id),
  required_mission_id INTEGER NOT NULL REFERENCES ptu_missions(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  PRIMARY KEY (mission_id, required_mission_id)
);
CREATE TABLE ptu_mission_reputation_requirements (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  mission_id INTEGER NOT NULL REFERENCES ptu_missions(id),
  faction_slug TEXT NOT NULL,
  scope_slug TEXT NOT NULL,
  comparison TEXT NOT NULL,
  standing_slug TEXT NOT NULL,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  data_source TEXT DEFAULT 'p4k',
  is_deleted INTEGER DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT,
  UNIQUE(mission_id, faction_slug, scope_slug, comparison, standing_slug, game_version_id)
);
CREATE TABLE ptu_missions (
  id                    INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                  TEXT NOT NULL,
  title                 TEXT,
  display_name          TEXT,
  description           TEXT,
  mission_type_id       INTEGER REFERENCES ptu_mission_types(id),
  mission_giver_id      INTEGER REFERENCES ptu_mission_givers(id),
  reward_amount         INTEGER DEFAULT 0,
  reward_currency       TEXT DEFAULT 'aUEC',
  reputation_scope_id   INTEGER REFERENCES ptu_reputation_scopes(id),
  reputation_reward_size TEXT,
  min_reputation        INTEGER,
  is_lawful             INTEGER,
  not_for_release       INTEGER DEFAULT 0,
  location_hint         TEXT,
  category              TEXT,
  subcategory           TEXT,
  difficulty            TEXT,
  game_version_id       INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, rep_fail_summary TEXT, rep_abandon_summary TEXT, time_limit_minutes INTEGER, max_players INTEGER, can_share INTEGER DEFAULT 1, once_only INTEGER DEFAULT 0, fail_if_criminal INTEGER, available_in_prison INTEGER DEFAULT 0, wanted_level_min INTEGER DEFAULT 0, wanted_level_max INTEGER DEFAULT 5, buy_in_amount INTEGER DEFAULT 0, reward_max INTEGER, has_standing_bonus INTEGER DEFAULT 0, location_ref TEXT, locality TEXT, [mission_giver] TEXT, [mission_type] TEXT, [name] TEXT, [reward_min] INTEGER, [slug] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, is_dynamic_reward INTEGER NOT NULL DEFAULT 0,
  UNIQUE(uuid)
);
CREATE TABLE ptu_mission_givers (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  faction_id        INTEGER REFERENCES ptu_factions(id),
  location_id       INTEGER REFERENCES ptu_star_map_locations(id),
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, biography TEXT, occupation TEXT, association TEXT, headquarters TEXT, portrait_url TEXT, is_lawful INTEGER, allies_json TEXT, enemies_json TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_mission_types (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  category          TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [icon_name] TEXT, [svg_icon_path] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_mission_organizations (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_star_map_locations (
  id                    INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                  TEXT    NOT NULL,
  name                  TEXT    NOT NULL,
  slug                  TEXT,
  description           TEXT,
  location_type         TEXT    NOT NULL,
  parent_uuid           TEXT,
  star_system_id        INTEGER REFERENCES ptu_star_systems(id),
  jurisdiction_id       INTEGER REFERENCES ptu_law_jurisdictions(id),
  is_scannable          INTEGER DEFAULT 0,
  hide_in_starmap       INTEGER DEFAULT 0,
  game_version_id       INTEGER NOT NULL REFERENCES game_versions(id),
  created_at            TEXT    DEFAULT (datetime('now')),
  updated_at            TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [affiliation] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_star_systems (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  code              TEXT,
  description       TEXT,
  galactic_x        REAL,
  galactic_y        REAL,
  galactic_z        REAL,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_ship_missiles (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  type            TEXT,
  sub_type        TEXT,
  size            INTEGER,
  grade           TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), missile_type TEXT, lock_time REAL, tracking_signal TEXT, damage REAL, damage_type TEXT, blast_radius REAL, speed REAL, lock_range REAL, ammo_count INTEGER, removed INTEGER NOT NULL DEFAULT 0, [manufacturer_code] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_reputation_perks (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  scope_id          INTEGER NOT NULL REFERENCES ptu_reputation_scopes(id),
  standing_id       INTEGER NOT NULL REFERENCES ptu_reputation_standings(id),
  perk_name         TEXT NOT NULL,
  display_name      TEXT,
  description       TEXT,
  reward_item_uuid  TEXT,
  reward_item_name  TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(scope_id, standing_id, perk_name)
);
CREATE TABLE ptu_reputation_standings (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  scope_id          INTEGER NOT NULL REFERENCES ptu_reputation_scopes(id),
  min_reputation    INTEGER NOT NULL,
  drift_reputation  INTEGER DEFAULT 0,
  drift_time_hours  REAL    DEFAULT 0,
  is_gated          INTEGER DEFAULT 0,
  perk_description  TEXT,
  sort_order        INTEGER,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [description] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid, scope_id)
);
CREATE TABLE ptu_faction_reputation_scopes (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  faction_id        INTEGER NOT NULL REFERENCES ptu_factions(id),
  reputation_scope_id INTEGER NOT NULL REFERENCES ptu_reputation_scopes(id),
  is_primary        INTEGER DEFAULT 0,
  source            TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(faction_id, reputation_scope_id)
);
CREATE TABLE ptu_reputation_scopes (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  scope_key         TEXT    NOT NULL,
  description       TEXT,
  faction_id        INTEGER REFERENCES ptu_factions(id),
  max_reputation    INTEGER,
  initial_reputation INTEGER DEFAULT 0,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_reputation_reward_tiers (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  size_code       TEXT NOT NULL,
  direction       TEXT NOT NULL,
  rep_amount      INTEGER NOT NULL,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(size_code, direction)
);
CREATE TABLE ptu_npc_loadout_items (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  loadout_id        INTEGER NOT NULL REFERENCES ptu_npc_loadouts(id) ON DELETE CASCADE,
  port_name         TEXT NOT NULL,
  item_name         TEXT NOT NULL,
  tag               TEXT,
  parent_port       TEXT,
  loot_map_uuid     TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id)
, is_hidden INTEGER DEFAULT 0, resolved_name TEXT, loot_item_id INTEGER, manufacturer_name TEXT, [item_uuid] TEXT, [loadout_uuid] TEXT, [slot] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_npc_loadouts (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  file_path         TEXT NOT NULL,
  loadout_name      TEXT NOT NULL,
  faction_id        INTEGER REFERENCES npc_factions(id),
  category          TEXT NOT NULL,
  sub_category      TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT DEFAULT (datetime('now')), visible_item_count INTEGER DEFAULT 0, removed INTEGER NOT NULL DEFAULT 0, [faction] TEXT, [item_count] INTEGER, [name] TEXT, [species] TEXT, [uuid] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(file_path)
);
CREATE TABLE ptu_mining_quality_distributions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    min_quality INTEGER NOT NULL DEFAULT 1,
    max_quality INTEGER NOT NULL DEFAULT 1000,
    mean REAL NOT NULL DEFAULT 500,
    stddev REAL NOT NULL DEFAULT 250,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_mining_modules (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    size INTEGER NOT NULL DEFAULT 1,
    grade INTEGER NOT NULL DEFAULT 1,
    manufacturer TEXT,
    charges INTEGER,
    lifetime REAL,
    damage_multiplier REAL NOT NULL DEFAULT 1.0,
    mod_instability REAL NOT NULL DEFAULT 0,
    mod_optimal_window_size REAL NOT NULL DEFAULT 0,
    mod_resistance REAL NOT NULL DEFAULT 0,
    mod_shatter_damage REAL NOT NULL DEFAULT 0,
    mod_cluster_factor REAL NOT NULL DEFAULT 0,
    mod_optimal_charge_rate REAL NOT NULL DEFAULT 0,
    mod_catastrophic_charge_rate REAL NOT NULL DEFAULT 0,
    mod_filter REAL NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_mining_locations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    preset_file TEXT NOT NULL,
    preset_guid TEXT,
    name TEXT NOT NULL,
    system TEXT NOT NULL,
    location_type TEXT NOT NULL,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, [deposits] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_mining_lasers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    size INTEGER NOT NULL DEFAULT 1,
    grade INTEGER NOT NULL DEFAULT 1,
    manufacturer TEXT,
    vehicle_built_in TEXT,
    module_slots INTEGER NOT NULL DEFAULT 0,
    throttle_lerp_speed REAL,
    throttle_minimum REAL,
    beam_full_range REAL,
    beam_zero_range REAL,
    beam_dps REAL,
    extract_full_range REAL,
    extract_zero_range REAL,
    extract_dps REAL,
    mod_instability REAL NOT NULL DEFAULT 0,
    mod_optimal_window_size REAL NOT NULL DEFAULT 0,
    mod_resistance REAL NOT NULL DEFAULT 0,
    mod_shatter_damage REAL NOT NULL DEFAULT 0,
    mod_cluster_factor REAL NOT NULL DEFAULT 0,
    mod_optimal_charge_rate REAL NOT NULL DEFAULT 0,
    mod_catastrophic_charge_rate REAL NOT NULL DEFAULT 0,
    mod_filter REAL NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_mining_gadgets (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    manufacturer TEXT,
    mod_instability REAL NOT NULL DEFAULT 0,
    mod_optimal_window_size REAL NOT NULL DEFAULT 0,
    mod_resistance REAL NOT NULL DEFAULT 0,
    mod_shatter_damage REAL NOT NULL DEFAULT 0,
    mod_cluster_factor REAL NOT NULL DEFAULT 0,
    mod_optimal_charge_rate REAL NOT NULL DEFAULT 0,
    mod_catastrophic_charge_rate REAL NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_mining_clustering_presets (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL,
    name TEXT NOT NULL,
    probability_of_clustering REAL NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, [params] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
    UNIQUE(uuid)
);
CREATE TABLE ptu_fps_weapons (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  class_name      TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  sub_type        TEXT,
  size            INTEGER,
  description     TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data        TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, rounds_per_minute REAL, fire_modes TEXT, burst_count INTEGER, ammo_capacity INTEGER, zoom_factor REAL, item_port_count INTEGER, damage REAL, damage_type TEXT, projectile_speed REAL, effective_range REAL, dps REAL, magazine_uuid TEXT, spread_min REAL, spread_max REAL, removed INTEGER NOT NULL DEFAULT 0, heat_per_shot REAL, charge_time REAL, recoil_strength REAL, weapon_class TEXT, [can_loot] INTEGER, [cooling_per_second] REAL, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [heal_range] REAL, [heal_rate] REAL, [heal_sensor_range] REAL, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [mining_throttle_speed] REAL, [overheat_fix_time] REAL, [overheat_temperature] REAL, [projectile_lifetime] REAL, [time_to_cooling_starts] REAL, [tool_type] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, has_builtin_scope INTEGER NOT NULL DEFAULT 0,
  UNIQUE(uuid)
);
CREATE TABLE ptu_fps_utilities (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  class_name      TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  sub_type        TEXT,
  description     TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data        TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, heal_amount REAL, effect_duration REAL, consumable_type TEXT, damage REAL, blast_radius REAL, fuse_time REAL, device_type TEXT, removed INTEGER NOT NULL DEFAULT 0, detonation_type TEXT, [can_loot] INTEGER, [grade] TEXT, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [size] INTEGER, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_fps_melee (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT NOT NULL,
  name              TEXT NOT NULL,
  display_name      TEXT,
  slug              TEXT,
  class_name        TEXT,
  manufacturer_id   INTEGER REFERENCES ptu_manufacturers(id),
  sub_type          TEXT,
  size              INTEGER,
  description       TEXT,
  damage            REAL,
  damage_type       TEXT,
  attack_speed      REAL,
  range             REAL,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT DEFAULT (datetime('now')),
  updated_at        TEXT DEFAULT (datetime('now')), heavy_damage REAL, attack_types REAL, can_block INTEGER, can_takedown INTEGER, removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_fps_helmets (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  sub_type        TEXT,
  size            REAL,
  grade           TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), resist_physical REAL, resist_energy REAL, resist_distortion REAL, resist_thermal REAL, resist_biochemical REAL, resist_stun REAL, ir_emission REAL, em_emission REAL, item_port_count INTEGER, atmosphere_capacity REAL, removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [has_visor_display] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [max_fov] REAL, [min_fov] REAL, [radiation_capacity] REAL, [radiation_dissipation] REAL, [temperature_max] REAL, [temperature_min] REAL, data_source TEXT, protected_body_parts TEXT, armor_weight TEXT, integrity_threshold REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_fps_clothing (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  slot            TEXT,
  size            REAL,
  grade           TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), resist_physical REAL, resist_energy REAL, resist_distortion REAL, resist_thermal REAL, resist_biochemical REAL, resist_stun REAL, ir_emission REAL, em_emission REAL, storage_capacity INTEGER, temperature_range_min REAL, temperature_range_max REAL, removed INTEGER NOT NULL DEFAULT 0, sub_type TEXT, [can_loot] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [item_port_count] INTEGER, [loot_rarity] TEXT, [manufacturer_code] TEXT, [radiation_capacity] REAL, [radiation_dissipation] REAL, [temperature_max] REAL, [temperature_min] REAL, data_source TEXT, protected_body_parts TEXT, armor_weight TEXT, integrity_threshold REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_fps_attachments (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  class_name      TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  sub_type        TEXT,
  size            INTEGER,
  description     TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data        TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, zoom_scale REAL, second_zoom_scale REAL, damage_multiplier REAL, sound_radius_multiplier REAL, scope_type TEXT, zeroing_range REAL, zeroing_increment REAL, auto_zeroing_time REAL, is_low_light INTEGER DEFAULT 0, recoil_decay REAL, recoil_strength REAL, recoil_randomness REAL, spread_min REAL, spread_max REAL, spread_attack_time REAL, spread_decay_time REAL, light_type TEXT, light_color TEXT, light_intensity REAL, light_radius REAL, light_fov REAL, removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [magazine_capacity] INTEGER, [magazine_restock_count] INTEGER, [manufacturer_code] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, fire_rate_multiplier REAL, projectile_speed_multiplier REAL, heat_generation_multiplier REAL,
  UNIQUE(uuid)
);
CREATE TABLE ptu_fps_armour (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  class_name      TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  sub_type        TEXT,
  size            INTEGER,
  grade           TEXT,
  description     TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data        TEXT,
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, resist_physical REAL, resist_energy REAL, resist_distortion REAL, resist_thermal REAL, resist_biochemical REAL, resist_stun REAL, ir_emission REAL, em_emission REAL, item_port_count INTEGER, removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [radiation_capacity] REAL, [radiation_dissipation] REAL, [temperature_max] REAL, [temperature_min] REAL, data_source TEXT, protected_body_parts TEXT, armor_weight TEXT, integrity_threshold REAL, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_fps_ammo_types (
  id                 INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid               TEXT NOT NULL,
  name               TEXT NOT NULL,
  display_name       TEXT,
  slug               TEXT,
  class_name         TEXT,
  caliber            TEXT,
  damage_per_round   REAL,
  damage_type        TEXT,
  projectile_speed   REAL,
  magazine_capacity  INTEGER,
  game_version_id    INTEGER NOT NULL REFERENCES game_versions(id),
  created_at         TEXT DEFAULT (datetime('now')), manufacturer_id INTEGER REFERENCES ptu_manufacturers(id), removed INTEGER NOT NULL DEFAULT 0, [ammo_type] TEXT, [damage_distortion] REAL, [damage_energy] REAL, [damage_physical] REAL, [speed] REAL, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, damage_thermal REAL, damage_biochemical REAL, damage_stun REAL,
  UNIQUE(uuid)
);
CREATE TABLE ptu_fps_carryables (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT NOT NULL,
  name              TEXT NOT NULL,
  display_name      TEXT,
  slug              TEXT,
  class_name        TEXT,
  sub_type          TEXT,
  mass              REAL,
  interaction_type  TEXT,
  value             REAL,
  description       TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [size] INTEGER, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_consumable_effects (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  consumable_uuid TEXT NOT NULL,
  effect_key TEXT NOT NULL,
  magnitude REAL,
  duration_seconds REAL,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(consumable_uuid, effect_key)
);
CREATE TABLE ptu_consumable_effect_types (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  key         TEXT    NOT NULL,
  name        TEXT    NOT NULL,
  description TEXT,
  is_positive INTEGER DEFAULT 1,
  game_version_id INTEGER REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(key)
);
CREATE TABLE ptu_consumables (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  type            TEXT,
  sub_type        TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [size] INTEGER, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_contract_generator_blueprint_pools (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  contract_generator_contract_id INTEGER NOT NULL REFERENCES ptu_contract_generator_contracts(id),
  crafting_blueprint_reward_pool_id INTEGER NOT NULL REFERENCES ptu_crafting_blueprint_reward_pools(id),
  chance REAL DEFAULT 1.0,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id)
, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_contract_generator_careers (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  contract_generator_id INTEGER NOT NULL REFERENCES ptu_contract_generators(id),
  debug_name TEXT NOT NULL,
  system TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id)
, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_contract_generator_contracts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  career_id INTEGER NOT NULL REFERENCES ptu_contract_generator_careers(id),
  uuid TEXT NOT NULL,
  debug_name TEXT NOT NULL,
  difficulty TEXT,
  template TEXT,
  min_standing TEXT,
  max_standing TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), rep_reward INTEGER, rep_rewards_json TEXT, title_loc_key TEXT, desc_loc_key TEXT, item_rewards_json TEXT, completion_tags_json TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_contract_generators (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  generator_key TEXT NOT NULL,
  display_name TEXT,
  faction_name TEXT,
  guild TEXT,
  mission_type TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), description TEXT, focus TEXT, headquarters TEXT, leadership TEXT, faction_slug TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(generator_key)
);
CREATE TABLE ptu_contract_blueprint_reward_pools (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    contract_generator_key TEXT NOT NULL,
    crafting_blueprint_reward_pool_id INTEGER NOT NULL REFERENCES ptu_crafting_blueprint_reward_pools(id),
    chance REAL NOT NULL DEFAULT 1.0,
    game_version_id INTEGER REFERENCES game_versions(id)
, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_contracts (
  id               INTEGER PRIMARY KEY AUTOINCREMENT,
  contract_key     TEXT    NOT NULL,
  giver            TEXT    NOT NULL,
  giver_slug       TEXT    NOT NULL,
  category         TEXT    NOT NULL,
  sequence_num     INTEGER,
  title            TEXT    NOT NULL,
  description      TEXT,
  reward_text      TEXT,
  reward_amount    INTEGER DEFAULT 0,
  reward_currency  TEXT,
  is_dynamic_reward INTEGER DEFAULT 0,
  is_active        INTEGER DEFAULT 1,
  notes            TEXT,
  game_version_id  INTEGER NOT NULL REFERENCES game_versions(id), requirements_json TEXT, removed INTEGER NOT NULL DEFAULT 0, reward_vehicle_slug TEXT, [name] TEXT, [slug] TEXT, [uuid] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(contract_key)
);
CREATE TABLE ptu_crafting_blueprint_reward_pools (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    key TEXT NOT NULL,
    name TEXT,
    game_version_id INTEGER REFERENCES game_versions(id), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
    UNIQUE(key)
);
CREATE TABLE ptu_crafting_resources (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, [key] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_crafting_blueprints (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    tag TEXT NOT NULL,
    name TEXT NOT NULL,
    type TEXT NOT NULL,
    sub_type TEXT NOT NULL,
    product_entity_class TEXT,
    craft_time_seconds INTEGER NOT NULL DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id)
, removed INTEGER NOT NULL DEFAULT 0, is_default INTEGER NOT NULL DEFAULT 0, category_id INTEGER REFERENCES crafting_blueprint_categories(id), [description] TEXT, [output_item] TEXT, [slug] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT);
CREATE TABLE ptu_armor_resistance_profiles (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  physical          REAL,
  energy            REAL,
  distortion        REAL,
  thermal           REAL,
  biochemical       REAL,
  stun              REAL,
  impact_force      REAL,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_jurisdiction_infraction_overrides (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  jurisdiction_id     INTEGER NOT NULL REFERENCES ptu_law_jurisdictions(id),
  infraction_id       INTEGER NOT NULL REFERENCES ptu_law_infractions(id),
  overrides_json      TEXT,
  game_version_id     INTEGER NOT NULL REFERENCES game_versions(id),
  created_at          TEXT    DEFAULT (datetime('now')),
  updated_at          TEXT    DEFAULT (datetime('now')), data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(jurisdiction_id, infraction_id)
);
CREATE TABLE ptu_law_jurisdictions (
  id                    INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                  TEXT    NOT NULL,
  name                  TEXT    NOT NULL,
  slug                  TEXT,
  parent_uuid           TEXT,
  respects_parent_laws  INTEGER DEFAULT 1,
  base_fine             INTEGER,
  is_prison             INTEGER DEFAULT 0,
  max_stolen_goods_scu  REAL,
  prohibited_goods_json TEXT,
  controlled_substances_json TEXT,
  game_version_id       INTEGER NOT NULL REFERENCES game_versions(id),
  created_at            TEXT    DEFAULT (datetime('now')),
  updated_at            TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_law_infractions (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  severity          TEXT    NOT NULL,
  triggers_json     TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), is_felony INTEGER, grace_allowance REAL, grace_allowance_cooldown REAL, grace_period REAL, grace_cooloff_scale REAL, display_grace_time INTEGER, escalated_fine_multiplier REAL, early_payment_period REAL, lifetime REAL, cool_off_time REAL, press_charges_notification_time REAL, remove_time_seconds REAL, felony_merits REAL, ignore_party_member INTEGER, hide_crime_notification INTEGER, hide_crime_journal INTEGER, removed INTEGER NOT NULL DEFAULT 0, [cool_off_seconds] REAL, [escalation_multiplier] REAL, [fine_amount] REAL, [lifetime_hours] REAL, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_harvestables (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  sub_type        TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_props (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  class_name      TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  description     TEXT,
  type            TEXT,
  sub_type        TEXT,
  manufacturer_id INTEGER REFERENCES ptu_manufacturers(id),
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      TEXT    DEFAULT (datetime('now')),
  updated_at      TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [manufacturer_code] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_trade_commodities (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid            TEXT NOT NULL,
    name            TEXT NOT NULL,
    slug            TEXT NOT NULL,
    class_name      TEXT,
    category        TEXT,        
    type_name       TEXT,        
    subtype_name    TEXT,        
    is_raw          INTEGER DEFAULT 0,  
    boxable         INTEGER DEFAULT 0,  
    scu_per_unit    REAL,        
    description     TEXT,
    game_version_id INTEGER REFERENCES game_versions(id),
    created_at      TEXT DEFAULT (datetime('now')),
    updated_at      TEXT DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, [sub_category] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
    UNIQUE(uuid)
);
CREATE TABLE ptu_refining_processes (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  speed             TEXT    NOT NULL,
  quality           TEXT    NOT NULL,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_damage_types (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_factions (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  description       TEXT,
  faction_type      TEXT,
  headquarters      TEXT,
  founded           TEXT,
  leadership        TEXT,
  area              TEXT,
  focus             TEXT,
  default_reaction  TEXT,
  can_arrest        INTEGER DEFAULT 0,
  polices_crime     INTEGER DEFAULT 0,
  no_legal_rights   INTEGER DEFAULT 0,
  allies_json       TEXT,
  enemies_json      TEXT,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_paints (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT,
    name TEXT NOT NULL,
    slug TEXT,
    class_name TEXT,
    description TEXT,
    image_url TEXT,
    image_url_small TEXT,
    image_url_medium TEXT,
    image_url_large TEXT,
    raw_data TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_base_variant INTEGER DEFAULT 0,
    game_version_id INTEGER REFERENCES game_versions(id), [can_loot] INTEGER, [grade] INTEGER, [grid_height] INTEGER, [grid_width] INTEGER, [inventory_volume] REAL, [loot_rarity] TEXT, [manufacturer_code] TEXT, [size] INTEGER, [sub_type] TEXT, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
    UNIQUE(class_name)
);
CREATE TABLE ptu_shops (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid              TEXT    NOT NULL,
  name              TEXT    NOT NULL,
  slug              TEXT,
  shop_type         TEXT,
  location_id       INTEGER REFERENCES ptu_star_map_locations(id),
  is_event          INTEGER DEFAULT 0,
  game_version_id   INTEGER NOT NULL REFERENCES game_versions(id),
  created_at        TEXT    DEFAULT (datetime('now')),
  updated_at        TEXT    DEFAULT (datetime('now')), location_label TEXT, franchise_uuid TEXT, display_name TEXT, inventory_type TEXT, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, component_type TEXT, scshop_label TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_vehicles (
  id                        INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                      TEXT,
  slug                      TEXT    NOT NULL,
  name                      TEXT    NOT NULL,
  class_name                TEXT,
  manufacturer_id           INTEGER REFERENCES ptu_manufacturers(id),
  vehicle_type_id           INTEGER REFERENCES vehicle_types(id),
  production_status_id      INTEGER REFERENCES production_statuses(id),
  size                      INTEGER,
  size_label                TEXT,
  focus                     TEXT,
  classification            TEXT,
  description               TEXT,
  length                    REAL,
  beam                      REAL,
  height                    REAL,
  mass                      REAL,
  cargo                     REAL,
  vehicle_inventory         REAL,
  crew_min                  INTEGER,
  crew_max                  INTEGER,
  speed_scm                 REAL,
  speed_max                 REAL,
  health                    REAL,
  pledge_price              REAL,
  price_auec                REAL,
  on_sale                   BOOLEAN DEFAULT FALSE,
  image_url                 TEXT,
  image_url_small           TEXT,
  image_url_medium          TEXT,
  image_url_large           TEXT,
  pledge_url                TEXT,
  game_version_id           INTEGER NOT NULL REFERENCES game_versions(id),
  raw_data                  TEXT,
  created_at                DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at                DATETIME DEFAULT CURRENT_TIMESTAMP,
  parent_vehicle_id         INTEGER REFERENCES ptu_vehicles(id),
  is_paint_variant          INTEGER NOT NULL DEFAULT 0,
  boost_speed_back          INTEGER,
  angular_velocity_pitch    REAL,
  angular_velocity_yaw      REAL,
  angular_velocity_roll     REAL,
  fuel_capacity_hydrogen    REAL,
  fuel_capacity_quantum     REAL,
  thruster_count_main       INTEGER,
  thruster_count_maneuvering INTEGER,
  acquisition_type          TEXT, acquisition_source_name TEXT, cross_section_x REAL, cross_section_y REAL, cross_section_z REAL, ir_signature REAL, em_signature REAL, removed INTEGER NOT NULL DEFAULT 0, armor_hp INTEGER DEFAULT 0, armor_damage_physical REAL DEFAULT 1, armor_damage_energy REAL DEFAULT 1, armor_damage_distortion REAL DEFAULT 1, armor_damage_thermal REAL DEFAULT 1, armor_deflection_physical REAL DEFAULT 0, armor_deflection_energy REAL DEFAULT 0, armor_signal_ir REAL DEFAULT 1, armor_signal_em REAL DEFAULT 1, armor_signal_cs REAL DEFAULT 1, weapon_pool_size INTEGER DEFAULT 0, shield_pool_max INTEGER DEFAULT 0, replaced_by_vehicle_id INTEGER REFERENCES ptu_vehicles(id), [manufacturer_code] TEXT, [vehicle_type] TEXT, data_source TEXT, claim_time REAL, expedited_claim_time REAL, expedited_claim_cost INTEGER, is_pledgeable INTEGER NOT NULL DEFAULT 1, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT, short_slug TEXT, is_variant INTEGER NOT NULL DEFAULT 0, is_purchasable_ingame INTEGER NOT NULL DEFAULT 0, g_force_resistance REAL, allow_room_connection INTEGER DEFAULT 0, is_npc_only INTEGER NOT NULL DEFAULT 0,
  UNIQUE(slug)
);
CREATE TABLE ptu_manufacturers (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid            TEXT    NOT NULL,
  name            TEXT    NOT NULL,
  slug            TEXT    NOT NULL,
  code            TEXT,
  known_for       TEXT,
  description     TEXT,
  logo_url        TEXT,
  raw_data        TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP, class TEXT, removed INTEGER NOT NULL DEFAULT 0, data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_crafting_blueprint_slots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    crafting_blueprint_id INTEGER NOT NULL REFERENCES ptu_crafting_blueprints(id),
    slot_index INTEGER NOT NULL DEFAULT 0,
    name TEXT NOT NULL,
    resource_name TEXT NOT NULL,
    quantity REAL NOT NULL DEFAULT 0,
    min_quality INTEGER NOT NULL DEFAULT 0,
    blueprint_uuid TEXT,
    slot_name TEXT,
    data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT
, slot_type TEXT NOT NULL DEFAULT 'resource', item_class TEXT);
CREATE TABLE ptu_crafting_slot_modifiers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    crafting_blueprint_slot_id INTEGER NOT NULL REFERENCES ptu_crafting_blueprint_slots(id),
    crafting_property_id INTEGER NOT NULL REFERENCES crafting_properties(id),
    start_quality INTEGER NOT NULL DEFAULT 0,
    end_quality INTEGER NOT NULL DEFAULT 1000,
    modifier_at_start REAL NOT NULL DEFAULT 1.0,
    modifier_at_end REAL NOT NULL DEFAULT 1.0,
    blueprint_uuid TEXT,
    property_id TEXT,
    slot_index INTEGER,
    data_source TEXT, is_deleted INTEGER DEFAULT 0, deleted_at TEXT, deleted_in_patch TEXT
);
CREATE TABLE crafting_quality_quantization (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    resource_name TEXT NOT NULL,
    bands_json TEXT NOT NULL,
    game_version_id INTEGER REFERENCES game_versions(id),
    data_source TEXT,
    is_deleted INTEGER DEFAULT 0,
    deleted_at TEXT,
    deleted_in_patch TEXT
);
CREATE TABLE transport_carriage_announcements (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    area_key TEXT,
    pre_arrival_alert_time REAL,
    pre_departure_alert_time REAL,
    post_departure_alert_time REAL,
    announcements_json TEXT,
    game_version_id INTEGER REFERENCES game_versions(id),
    data_source TEXT,
    is_deleted INTEGER DEFAULT 0,
    deleted_at TEXT,
    deleted_in_patch TEXT
);
CREATE TABLE transport_destination_categories (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    categories_json TEXT NOT NULL,
    game_version_id INTEGER REFERENCES game_versions(id),
    data_source TEXT,
    is_deleted INTEGER DEFAULT 0,
    deleted_at TEXT,
    deleted_in_patch TEXT
);
CREATE TABLE unified_shake_params (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    name TEXT,
    position_amplitude_json TEXT,
    rotation_amplitude_json TEXT,
    first_person_scale REAL,
    third_person_scale REAL,
    game_version_id INTEGER REFERENCES game_versions(id),
    data_source TEXT,
    is_deleted INTEGER DEFAULT 0,
    deleted_at TEXT,
    deleted_in_patch TEXT
);
CREATE TABLE ptu_crafting_quality_quantization (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    resource_name TEXT NOT NULL,
    bands_json TEXT NOT NULL,
    game_version_id INTEGER REFERENCES game_versions(id),
    data_source TEXT,
    is_deleted INTEGER DEFAULT 0,
    deleted_at TEXT,
    deleted_in_patch TEXT
);
CREATE TABLE ptu_transport_carriage_announcements (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    area_key TEXT,
    pre_arrival_alert_time REAL,
    pre_departure_alert_time REAL,
    post_departure_alert_time REAL,
    announcements_json TEXT,
    game_version_id INTEGER REFERENCES game_versions(id),
    data_source TEXT,
    is_deleted INTEGER DEFAULT 0,
    deleted_at TEXT,
    deleted_in_patch TEXT
);
CREATE TABLE ptu_transport_destination_categories (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    categories_json TEXT NOT NULL,
    game_version_id INTEGER REFERENCES game_versions(id),
    data_source TEXT,
    is_deleted INTEGER DEFAULT 0,
    deleted_at TEXT,
    deleted_in_patch TEXT
);
CREATE TABLE ptu_unified_shake_params (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    uuid TEXT NOT NULL UNIQUE,
    name TEXT,
    position_amplitude_json TEXT,
    rotation_amplitude_json TEXT,
    first_person_scale REAL,
    third_person_scale REAL,
    game_version_id INTEGER REFERENCES game_versions(id),
    data_source TEXT,
    is_deleted INTEGER DEFAULT 0,
    deleted_at TEXT,
    deleted_in_patch TEXT
);
CREATE TABLE ptu_crafting_blueprint_reward_pool_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    crafting_blueprint_reward_pool_id INTEGER NOT NULL REFERENCES ptu_crafting_blueprint_reward_pools(id),
    crafting_blueprint_id INTEGER NOT NULL REFERENCES ptu_crafting_blueprints(id),
    weight INTEGER NOT NULL DEFAULT 1,
    pool_key TEXT,
    blueprint_uuid TEXT
);
CREATE TABLE "user_loot_collection" (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id      TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    loot_map_id  INTEGER,                    -- nullable, advisory only
    loot_uuid    TEXT    NOT NULL,
    collected_at TEXT    DEFAULT (datetime('now')),
    quantity     INTEGER NOT NULL DEFAULT 1
);
CREATE TABLE "user_loot_wishlist" (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id     TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    loot_map_id INTEGER,
    loot_uuid   TEXT    NOT NULL,
    added_at    TEXT    DEFAULT (datetime('now')),
    quantity    INTEGER NOT NULL DEFAULT 1
);
CREATE TABLE user_blueprint_builds (
    id                   INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id              TEXT    NOT NULL REFERENCES user(id) ON DELETE CASCADE,
    blueprint_uuid       TEXT    NOT NULL,
    name                 TEXT    NOT NULL,
    quality_config_json  TEXT    NOT NULL,
    crafted_quantity     INTEGER NOT NULL DEFAULT 0,
    notes                TEXT,
    created_at           TEXT    DEFAULT (datetime('now')),
    updated_at           TEXT    DEFAULT (datetime('now'))
);
CREATE TABLE pledge_item_media (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  title             TEXT    NOT NULL,
  title_lower       TEXT    NOT NULL,
  cf_image_id       TEXT    NOT NULL,
  cf_image_url      TEXT    NOT NULL,
  source_capture_id INTEGER REFERENCES image_captures(id),
  uploaded_by       TEXT    REFERENCES user(id) ON DELETE SET NULL,
  uploaded_at       TEXT    NOT NULL DEFAULT (datetime('now')),
  notes             TEXT
, title_norm TEXT NOT NULL DEFAULT '');
CREATE TABLE mineable_elements (
  id                          INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                        TEXT    NOT NULL,
  name                        TEXT    NOT NULL,
  slug                        TEXT,
  class_name                  TEXT,
  category                    TEXT,
  commodity_id                INTEGER,
  game_version_id             INTEGER NOT NULL REFERENCES game_versions(id),
  created_at                  TEXT    DEFAULT (datetime('now')),
  updated_at                  TEXT    DEFAULT (datetime('now')),
  instability                 REAL,
  resistance                  REAL,
  optimal_window_midpoint     REAL,
  optimal_window_randomness   REAL,
  optimal_window_thinness     REAL,
  explosion_multiplier        REAL,
  cluster_factor              REAL,
  removed                     INTEGER NOT NULL DEFAULT 0,
  resource_type               TEXT,
  data_source                 TEXT,
  optimal_midpoint            REAL,
  is_deleted                  INTEGER DEFAULT 0,
  deleted_at                  TEXT,
  deleted_in_patch            TEXT,
  UNIQUE(uuid)
);
CREATE TABLE ptu_mineable_elements (
  id                          INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid                        TEXT    NOT NULL,
  name                        TEXT    NOT NULL,
  slug                        TEXT,
  class_name                  TEXT,
  category                    TEXT,
  commodity_id                INTEGER,
  game_version_id             INTEGER NOT NULL REFERENCES game_versions(id),
  created_at                  TEXT    DEFAULT (datetime('now')),
  updated_at                  TEXT    DEFAULT (datetime('now')),
  instability                 REAL,
  resistance                  REAL,
  optimal_window_midpoint     REAL,
  optimal_window_randomness   REAL,
  optimal_window_thinness     REAL,
  explosion_multiplier        REAL,
  cluster_factor              REAL,
  removed                     INTEGER NOT NULL DEFAULT 0,
  optimal_midpoint            REAL,
  resource_type               TEXT,
  data_source                 TEXT,
  is_deleted                  INTEGER DEFAULT 0,
  deleted_at                  TEXT,
  deleted_in_patch            TEXT,
  UNIQUE(uuid)
);
CREATE TABLE vehicle_storage (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),
  storage_type TEXT NOT NULL,
  container_class_name TEXT,
  scu_capacity REAL,
  microscu_capacity REAL,
  count INTEGER DEFAULT 1,
  location_label TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  data_source TEXT,
  is_deleted INTEGER NOT NULL DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT
);
CREATE TABLE ptu_vehicle_storage (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  vehicle_id INTEGER NOT NULL REFERENCES ptu_vehicles(id),
  storage_type TEXT NOT NULL,
  container_class_name TEXT,
  scu_capacity REAL,
  microscu_capacity REAL,
  count INTEGER DEFAULT 1,
  location_label TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  data_source TEXT,
  is_deleted INTEGER NOT NULL DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT
);
CREATE TABLE mission_rep_changes (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  mission_id INTEGER NOT NULL REFERENCES missions(id),
  scope_slug TEXT NOT NULL,              -- 'security', 'affinity', etc.
  event TEXT NOT NULL,                   -- 'fail' | 'abandon' (success added when CIG ships a summary for it)
  size_code TEXT NOT NULL,               -- 'XXS' | 'XS' | 'S' | 'M' | 'L' | 'XL' | 'XXL'
  direction TEXT NOT NULL,               -- 'positive' | 'negative'
  rep_amount INTEGER,                    -- resolved from reputation_reward_tiers; nullable
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  data_source TEXT,
  UNIQUE(mission_id, scope_slug, event, game_version_id)
);
CREATE TABLE user_localization_overrides (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id     TEXT NOT NULL,
  loc_key     TEXT NOT NULL,
  value       TEXT NOT NULL,
  created_at  TEXT DEFAULT (datetime('now')),
  updated_at  TEXT DEFAULT (datetime('now')),
  UNIQUE(user_id, loc_key)
);
CREATE TABLE pack_requests (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  url           TEXT NOT NULL,
  note          TEXT,
  status        TEXT NOT NULL DEFAULT 'new',
  created_at    TEXT DEFAULT (datetime('now'))
);
CREATE TABLE ptu_terminal_inventory (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  terminal_id INTEGER NOT NULL REFERENCES ptu_terminals(id),
  item_uuid TEXT NOT NULL,
  item_type TEXT,
  item_name TEXT,
  base_buy_price REAL,
  base_sell_price REAL,
  latest_buy_price REAL,
  latest_sell_price REAL,
  latest_source TEXT,
  latest_observed_at TEXT,
  base_inventory REAL,
  max_inventory REAL,
  game_version_id INTEGER REFERENCES game_versions(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now')),
  is_deleted INTEGER DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT,
  uex_date_modified INTEGER,
  uex_date_added INTEGER,
  UNIQUE(terminal_id, item_uuid)
);
CREATE TABLE ptu_rock_compositions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid TEXT NOT NULL UNIQUE,
  name TEXT NOT NULL,
  slug TEXT,
  class_name TEXT,
  rock_type TEXT,
  min_elements INTEGER,
  composition_json TEXT,
  deposit_name TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now')),
  removed INTEGER NOT NULL DEFAULT 0,
  data_source TEXT,
  is_deleted INTEGER DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT
);
CREATE TABLE mineable_rock_entities (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid TEXT NOT NULL UNIQUE,
  class_name TEXT NOT NULL,
  composition_uuid TEXT NOT NULL,
  rock_category TEXT,
  laser_damage_full_value REAL,
  damage_strength_power_curve REAL,
  filled_factor REAL,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  removed INTEGER NOT NULL DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now')),
  is_deleted INTEGER DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT
, data_source TEXT);
CREATE TABLE ptu_mineable_rock_entities (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  uuid TEXT NOT NULL UNIQUE,
  class_name TEXT NOT NULL,
  composition_uuid TEXT NOT NULL,
  rock_category TEXT,
  laser_damage_full_value REAL,
  damage_strength_power_curve REAL,
  filled_factor REAL,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  removed INTEGER NOT NULL DEFAULT 0,
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now')),
  is_deleted INTEGER DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT
, data_source TEXT);
CREATE TABLE mining_global_params (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  scope TEXT NOT NULL UNIQUE,
  -- Top-level numeric fields from miningglobalparams*.json
  power_capacity_per_mass REAL,
  decay_per_mass REAL,
  optimal_window_size REAL,
  optimal_window_factor REAL,
  optimal_window_thinness_curve_factor REAL,
  optimal_window_max_size REAL,
  resistance_curve_factor REAL,
  controlled_breaking_fill_rate REAL,
  controlled_breaking_fill_rate_danger REAL,
  controlled_breaking_decay_rate REAL,
  danger_breaking_fill_rate REAL,
  danger_breaking_fill_rate_exponent REAL,
  danger_breaking_decay_rate REAL,
  cscu_per_volume REAL,
  -- mineableInstabilityParams (nested in source JSON, flattened here)
  instability_wave_period REAL,
  instability_wave_variance REAL,
  instability_curve_factor REAL,
  -- mineableExplosionParams -- keep the full nested ExplosionParams block as
  -- JSON for now. We don't need the inner shape until explosion math is
  -- wired into the Calculator.
  explosion_params_json TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
, data_source TEXT);
CREATE TABLE ptu_mining_global_params (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  scope TEXT NOT NULL UNIQUE,
  power_capacity_per_mass REAL,
  decay_per_mass REAL,
  optimal_window_size REAL,
  optimal_window_factor REAL,
  optimal_window_thinness_curve_factor REAL,
  optimal_window_max_size REAL,
  resistance_curve_factor REAL,
  controlled_breaking_fill_rate REAL,
  controlled_breaking_fill_rate_danger REAL,
  controlled_breaking_decay_rate REAL,
  danger_breaking_fill_rate REAL,
  danger_breaking_fill_rate_exponent REAL,
  danger_breaking_decay_rate REAL,
  cscu_per_volume REAL,
  instability_wave_period REAL,
  instability_wave_variance REAL,
  instability_curve_factor REAL,
  explosion_params_json TEXT,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
, data_source TEXT);
CREATE TABLE component_fuel_nozzles (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  hydrogen_flow_rate REAL,
  quantum_flow_rate REAL,
  max_integrity REAL,
  is_deleted INTEGER DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ptu_component_fuel_nozzles (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  component_id INTEGER NOT NULL REFERENCES ptu_vehicle_components(id) ON DELETE CASCADE,
  game_version_id INTEGER NOT NULL REFERENCES game_versions(id),
  hydrogen_flow_rate REAL,
  quantum_flow_rate REAL,
  max_integrity REAL,
  is_deleted INTEGER DEFAULT 0,
  deleted_at TEXT,
  deleted_in_patch TEXT,
  UNIQUE(component_id)
);
CREATE TABLE ai_chats (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id TEXT NOT NULL REFERENCES user(id) ON DELETE CASCADE,
  provider TEXT NOT NULL,
  model TEXT NOT NULL,
  title TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE TABLE ai_chat_messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  chat_id INTEGER NOT NULL REFERENCES ai_chats(id) ON DELETE CASCADE,
  role TEXT NOT NULL,            -- 'user' | 'assistant'
  content TEXT NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
-- ── indexes ──
CREATE INDEX idx_sync_history_started_at ON sync_history(started_at);
CREATE INDEX idx_invite_tokens_token ON invite_tokens(token);
CREATE INDEX idx_paint_vehicles_paint_id ON paint_vehicles(paint_id);
CREATE INDEX idx_user_fleet_user_id    ON user_fleet(user_id);
CREATE INDEX idx_user_fleet_vehicle_id ON user_fleet(vehicle_id);
CREATE INDEX idx_rsi_media_cf ON rsi_media(cf_image_id) WHERE cf_image_id IS NOT NULL;
CREATE INDEX idx_vehicle_rsi_meta_rsi_ship ON vehicle_rsi_meta(rsi_ship_id)
    WHERE rsi_ship_id IS NOT NULL;
CREATE INDEX idx_rsi_entity_mappings_entity ON rsi_entity_mappings(entity_table, entity_id)
    WHERE entity_table IS NOT NULL;
CREATE INDEX idx_rsi_entity_mappings_rsi_id ON rsi_entity_mappings(rsi_item_id)
    WHERE rsi_item_id IS NOT NULL;
CREATE INDEX idx_rsi_entity_mappings_pending ON rsi_entity_mappings(reviewed_at)
    WHERE reviewed_at IS NULL;
CREATE INDEX idx_rsi_staging_items_pending ON rsi_staging_items(reviewed_at)
    WHERE reviewed_at IS NULL;
CREATE INDEX idx_user_hangar_syncs_user ON user_hangar_syncs(user_id);
CREATE INDEX idx_user_pledges_user ON user_pledges(user_id);
CREATE INDEX idx_user_pledges_sync ON user_pledges(sync_id);
CREATE INDEX idx_user_pledge_items_user ON user_pledge_items(user_id);
CREATE INDEX idx_user_pledge_items_pledge ON user_pledge_items(user_pledge_id);
CREATE INDEX idx_user_pledge_items_kind ON user_pledge_items(kind);
CREATE INDEX idx_user_pledge_upgrades_user ON user_pledge_upgrades(user_id);
CREATE INDEX idx_user_pledge_upgrades_pledge ON user_pledge_upgrades(user_pledge_id);
CREATE INDEX idx_user_account_snapshots_user ON user_account_snapshots(user_id);
CREATE INDEX idx_user_account_snapshots_sync ON user_account_snapshots(sync_id);
CREATE INDEX idx_user_named_ships_user ON user_named_ships(user_id);
CREATE INDEX idx_user_change_history_user_id    ON user_change_history(user_id);
CREATE INDEX idx_user_change_history_event_type ON user_change_history(event_type_id);
CREATE INDEX idx_user_change_history_created_at ON user_change_history(created_at);
CREATE INDEX idx_npc_loadout_items_loadout ON npc_loadout_items(loadout_id);
CREATE INDEX idx_npc_loadout_items_item ON npc_loadout_items(item_name);
CREATE INDEX idx_npc_loadout_items_version ON npc_loadout_items(game_version_id);
CREATE INDEX idx_loot_item_locations_source_key ON loot_item_locations(source_type, location_key);
CREATE INDEX idx_loot_item_locations_loot_map ON loot_item_locations(loot_map_id);
CREATE INDEX idx_loot_item_locations_version ON loot_item_locations(game_version_id);
CREATE INDEX idx_npc_loadout_items_hidden ON npc_loadout_items(is_hidden);
CREATE INDEX idx_user_buyback_user ON user_buyback_pledges(user_id);
CREATE INDEX idx_user_upgrades_user ON user_pledge_upgrades(user_id);
CREATE INDEX idx_loot_item_locations_map_source
  ON loot_item_locations(loot_map_id, source_type);
CREATE INDEX idx_org_verification_pending_user ON org_verification_pending(user_id);
CREATE INDEX idx_org_join_codes_org ON org_join_codes(organization_id);
CREATE INDEX idx_org_join_codes_code ON org_join_codes(code);
CREATE INDEX idx_user_localization_ship_order_user
  ON user_localization_ship_order(user_id);
CREATE INDEX idx_crafting_blueprints_type ON crafting_blueprints(type);
CREATE INDEX idx_crafting_blueprints_sub_type ON crafting_blueprints(sub_type);
CREATE INDEX idx_crafting_blueprints_game_version ON crafting_blueprints(game_version_id);
CREATE INDEX idx_crafting_blueprint_slots_blueprint ON crafting_blueprint_slots(crafting_blueprint_id);
CREATE INDEX idx_crafting_slot_modifiers_slot ON crafting_slot_modifiers(crafting_blueprint_slot_id);
CREATE INDEX idx_mining_locations_system ON mining_locations(system);
CREATE INDEX idx_mining_locations_game_version ON mining_locations(game_version_id);
CREATE INDEX idx_mining_location_deposits_location ON mining_location_deposits(mining_location_id);
CREATE INDEX idx_mining_clustering_params_preset ON mining_clustering_params(mining_clustering_preset_id);
CREATE INDEX idx_salvageable_ships_base ON salvageable_ships(base_vehicle_id);
CREATE INDEX idx_salvageable_ships_version ON salvageable_ships(game_version_id);
CREATE INDEX idx_salvageable_ship_components_ship ON salvageable_ship_components(salvageable_ship_id);
CREATE INDEX idx_salvageable_ship_components_uuid ON salvageable_ship_components(component_uuid);
CREATE INDEX idx_profile_verification_pending_handle ON profile_verification_pending(handle);
CREATE UNIQUE INDEX idx_user_rsi_profile_verified_handle
  ON user_rsi_profile(verified_handle) WHERE verified_handle IS NOT NULL;
CREATE INDEX idx_org_ops_org_id ON org_ops(org_id);
CREATE INDEX idx_org_ops_status ON org_ops(status);
CREATE INDEX idx_org_ops_created_by ON org_ops(created_by);
CREATE UNIQUE INDEX idx_org_ops_join_code ON org_ops(join_code) WHERE join_code IS NOT NULL;
CREATE INDEX idx_org_op_participants_op ON org_op_participants(org_op_id);
CREATE INDEX idx_org_op_participants_user ON org_op_participants(user_id);
CREATE INDEX idx_org_op_ships_op ON org_op_ships(org_op_id);
CREATE INDEX idx_org_op_capital_op ON org_op_capital(org_op_id);
CREATE INDEX idx_org_op_earnings_op ON org_op_earnings(org_op_id);
CREATE INDEX idx_org_op_payouts_op ON org_op_payouts(org_op_id);
CREATE INDEX idx_org_op_payouts_user ON org_op_payouts(user_id);
CREATE INDEX idx_player_ratings_ratee ON player_ratings(ratee_user_id);
CREATE INDEX idx_player_ratings_rater ON player_ratings(rater_user_id);
CREATE INDEX idx_player_ratings_op ON player_ratings(org_op_id);
CREATE INDEX idx_player_reviews_ratee ON player_reviews(ratee_user_id);
CREATE INDEX idx_rating_audit_log_actor ON rating_audit_log(actor_user_id);
CREATE INDEX idx_rating_audit_log_created ON rating_audit_log(created_at);
CREATE INDEX idx_player_reputation_user ON player_reputation(user_id);
CREATE INDEX idx_companion_events_user    ON companion_events(user_id);
CREATE INDEX idx_companion_events_type    ON companion_events(user_id, type);
CREATE INDEX idx_companion_events_time    ON companion_events(user_id, event_timestamp);
CREATE INDEX idx_mining_location_deposits_composition ON mining_location_deposits(rock_composition_id);
CREATE INDEX idx_ufl_user ON user_fleet_loadout(user_id);
CREATE INDEX idx_ufl_fleet ON user_fleet_loadout(user_fleet_id);
CREATE INDEX idx_ulc_user ON user_loadout_cart(user_id);
CREATE INDEX idx_crafting_bp_reward_items_pool ON crafting_blueprint_reward_pool_items(crafting_blueprint_reward_pool_id);
CREATE INDEX idx_crafting_bp_reward_items_bp ON crafting_blueprint_reward_pool_items(crafting_blueprint_id);
CREATE INDEX idx_user_blueprints_user ON user_blueprints(user_id);
CREATE INDEX idx_contract_bp_pools_generator ON contract_blueprint_reward_pools(contract_generator_key);
CREATE INDEX idx_contract_bp_pools_pool ON contract_blueprint_reward_pools(crafting_blueprint_reward_pool_id);
CREATE INDEX idx_contract_bp_pools_version ON contract_blueprint_reward_pools(game_version_id);
CREATE INDEX idx_image_captures_vehicle ON image_captures(vehicle_id);
CREATE INDEX idx_image_captures_promoted ON image_captures(promoted);
CREATE INDEX idx_image_captures_kind ON image_captures(kind);
CREATE INDEX idx_companion_sessions_user ON companion_sessions(user_id);
CREATE INDEX idx_companion_sessions_time ON companion_sessions(user_id, started_at);
CREATE INDEX idx_companion_ship_events_user ON companion_ship_events(user_id);
CREATE INDEX idx_companion_ship_events_type ON companion_ship_events(user_id, event_type);
CREATE INDEX idx_companion_ship_events_time ON companion_ship_events(user_id, event_at);
CREATE INDEX idx_companion_mission_events_user ON companion_mission_events(user_id);
CREATE INDEX idx_companion_mission_events_type ON companion_mission_events(user_id, event_type);
CREATE INDEX idx_companion_mission_events_time ON companion_mission_events(user_id, event_at);
CREATE INDEX idx_companion_location_events_user ON companion_location_events(user_id);
CREATE INDEX idx_companion_location_events_type ON companion_location_events(user_id, event_type);
CREATE INDEX idx_companion_location_events_time ON companion_location_events(user_id, event_at);
CREATE INDEX idx_companion_travel_events_user ON companion_travel_events(user_id);
CREATE INDEX idx_companion_travel_events_time ON companion_travel_events(user_id, event_at);
CREATE INDEX idx_companion_economy_events_user ON companion_economy_events(user_id);
CREATE INDEX idx_companion_economy_events_type ON companion_economy_events(user_id, event_type);
CREATE INDEX idx_companion_economy_events_time ON companion_economy_events(user_id, event_at);
CREATE INDEX idx_companion_combat_events_user ON companion_combat_events(user_id);
CREATE INDEX idx_companion_combat_events_type ON companion_combat_events(user_id, event_type);
CREATE INDEX idx_companion_combat_events_time ON companion_combat_events(user_id, event_at);
CREATE INDEX idx_companion_social_events_user ON companion_social_events(user_id);
CREATE INDEX idx_companion_social_events_time ON companion_social_events(user_id, event_at);
CREATE INDEX idx_companion_system_events_user ON companion_system_events(user_id);
CREATE INDEX idx_companion_system_events_time ON companion_system_events(user_id, event_at);
CREATE INDEX idx_cgbp_contract_id ON contract_generator_blueprint_pools(contract_generator_contract_id);
CREATE INDEX idx_terminals_shop ON terminals(shop_id);
CREATE INDEX idx_terminals_type ON terminals(terminal_type);
CREATE INDEX idx_terminals_uex ON terminals(uex_terminal_id);
CREATE INDEX idx_price_observations_terminal ON price_observations(terminal_id);
CREATE INDEX idx_price_observations_item ON price_observations(item_uuid);
CREATE INDEX idx_price_observations_observed ON price_observations(observed_at);
CREATE INDEX idx_component_coolers_cid ON component_coolers(component_id);
CREATE INDEX idx_component_mining_cid ON component_mining(component_id);
CREATE INDEX idx_component_powerplants_cid ON component_powerplants(component_id);
CREATE INDEX idx_component_qed_cid ON component_qed(component_id);
CREATE INDEX idx_component_quantum_drives_cid ON component_quantum_drives(component_id);
CREATE INDEX idx_component_radar_cid ON component_radar(component_id);
CREATE INDEX idx_component_shields_cid ON component_shields(component_id);
CREATE INDEX idx_component_thrusters_cid ON component_thrusters(component_id);
CREATE INDEX idx_component_turrets_cid ON component_turrets(component_id);
CREATE INDEX idx_component_weapons_cid ON component_weapons(component_id);
CREATE INDEX idx_consumable_effects_uuid ON consumable_effects(consumable_uuid);
CREATE INDEX idx_consumable_effects_version ON consumable_effects(game_version_id);
CREATE INDEX idx_consumables_slug     ON consumables(slug);
CREATE INDEX idx_consumables_sub_type ON consumables(sub_type);
CREATE INDEX idx_consumables_type     ON consumables(type);
CREATE UNIQUE INDEX idx_cgbp_upsert
  ON contract_generator_blueprint_pools(contract_generator_contract_id, crafting_blueprint_reward_pool_id);
CREATE UNIQUE INDEX idx_cgc_upsert
  ON contract_generator_careers(debug_name, contract_generator_id);
CREATE INDEX idx_cgc_career_id ON contract_generator_contracts(career_id);
CREATE INDEX idx_cgc_desc_loc_key ON contract_generator_contracts(desc_loc_key);
CREATE INDEX idx_cg_faction_slug ON contract_generators(faction_slug, game_version_id);
CREATE INDEX idx_cg_generator_key ON contract_generators(generator_key);
CREATE INDEX idx_contracts_giver_slug ON contracts(giver_slug);
CREATE INDEX idx_contracts_is_active  ON contracts(is_active);
CREATE INDEX idx_crafting_bp_reward_pools_version ON crafting_blueprint_reward_pools(game_version_id);
CREATE INDEX idx_faction_reputation_scopes_faction ON faction_reputation_scopes(faction_id);
CREATE INDEX idx_faction_reputation_scopes_scope ON faction_reputation_scopes(reputation_scope_id);
CREATE INDEX idx_faction_reputation_scopes_version ON faction_reputation_scopes(game_version_id);
CREATE INDEX idx_factions_slug ON factions(slug);
CREATE INDEX idx_factions_type ON factions(faction_type);
CREATE INDEX idx_fps_ammo_types_version ON fps_ammo_types(game_version_id);
CREATE INDEX idx_fps_carryables_version ON fps_carryables(game_version_id);
CREATE INDEX idx_fps_clothing_slot ON fps_clothing(slot);
CREATE INDEX idx_fps_clothing_slug ON fps_clothing(slug);
CREATE INDEX idx_fps_helmets_slug     ON fps_helmets(slug);
CREATE INDEX idx_fps_helmets_sub_type ON fps_helmets(sub_type);
CREATE INDEX idx_fps_melee_manufacturer ON fps_melee(manufacturer_id);
CREATE INDEX idx_fps_melee_version ON fps_melee(game_version_id);
CREATE INDEX idx_harvestables_slug ON harvestables(slug);
CREATE INDEX idx_jio_infraction ON jurisdiction_infraction_overrides(infraction_id);
CREATE INDEX idx_jio_jurisdiction ON jurisdiction_infraction_overrides(jurisdiction_id);
CREATE INDEX idx_law_infractions_severity ON law_infractions(severity);
CREATE INDEX idx_law_infractions_slug ON law_infractions(slug);
CREATE INDEX idx_law_jurisdictions_slug ON law_jurisdictions(slug);
CREATE INDEX idx_loot_map_category ON loot_map(category);
CREATE INDEX idx_loot_map_class_name ON loot_map(class_name);
CREATE INDEX idx_loot_map_consumable   ON loot_map(consumable_id);
CREATE INDEX idx_loot_map_fps_armour_id ON loot_map(fps_armour_id);
CREATE INDEX idx_loot_map_fps_carryable ON loot_map(fps_carryable_id);
CREATE INDEX idx_loot_map_fps_clothing_id ON loot_map(fps_clothing_id);
CREATE INDEX idx_loot_map_fps_helmet_id ON loot_map(fps_helmet_id);
CREATE INDEX idx_loot_map_fps_melee ON loot_map(fps_melee_id);
CREATE INDEX idx_loot_map_fps_weapon_id ON loot_map(fps_weapon_id);
CREATE INDEX idx_loot_map_game_version ON loot_map(game_version_id);
CREATE INDEX idx_loot_map_harvestable  ON loot_map(harvestable_id);
CREATE INDEX idx_loot_map_props        ON loot_map(props_id);
CREATE INDEX idx_loot_map_rarity       ON loot_map(rarity);
CREATE INDEX idx_loot_map_ship_missile ON loot_map(ship_missile_id);
CREATE INDEX idx_loot_map_sub_type     ON loot_map(sub_type);
CREATE INDEX idx_loot_map_type         ON loot_map(type);
CREATE INDEX idx_loot_map_uuid_gv ON loot_map(uuid, game_version_id);
CREATE INDEX idx_loot_map_vehicle_component_id ON loot_map(vehicle_component_id);
CREATE INDEX idx_mission_givers_faction ON mission_givers(faction_id);
CREATE INDEX idx_mission_givers_location ON mission_givers(location_id);
CREATE INDEX idx_mission_givers_slug ON mission_givers(slug);
CREATE INDEX idx_mission_organizations_slug ON mission_organizations(slug);
CREATE INDEX idx_mp_required ON mission_prerequisites(required_mission_id);
CREATE INDEX idx_mission_types_category ON mission_types(category);
CREATE INDEX idx_missions_category ON missions(category);
CREATE INDEX idx_missions_giver ON missions(mission_giver_id);
CREATE INDEX idx_missions_reputation ON missions(reputation_scope_id);
CREATE INDEX idx_missions_type ON missions(mission_type_id);
CREATE INDEX idx_missions_version ON missions(game_version_id);
CREATE INDEX idx_npc_loadouts_faction ON npc_loadouts(faction_id);
CREATE INDEX idx_npc_loadouts_version ON npc_loadouts(game_version_id);
CREATE INDEX idx_paints_class_name ON paints(class_name);
CREATE INDEX idx_paints_slug ON paints(slug);
CREATE INDEX idx_paints_version ON paints(game_version_id);
CREATE INDEX idx_props_slug     ON props(slug);
CREATE INDEX idx_props_sub_type ON props(sub_type);
CREATE INDEX idx_reputation_perks_scope ON reputation_perks(scope_id);
CREATE INDEX idx_reputation_perks_version ON reputation_perks(game_version_id);
CREATE INDEX idx_reputation_reward_tiers_version ON reputation_reward_tiers(game_version_id);
CREATE INDEX idx_reputation_scopes_key ON reputation_scopes(scope_key);
CREATE INDEX idx_reputation_standings_scope ON reputation_standings(scope_id);
CREATE INDEX idx_rock_compositions_rock_type ON rock_compositions(rock_type);
CREATE UNIQUE INDEX idx_salvageable_ships_uuid_version ON salvageable_ships(entity_uuid);
CREATE INDEX idx_ship_missiles_size ON ship_missiles(size);
CREATE INDEX idx_ship_missiles_type ON ship_missiles(type, sub_type);
CREATE INDEX idx_ship_missiles_uuid ON ship_missiles(uuid);
CREATE INDEX idx_shop_locations_location ON shop_locations(location_id);
CREATE INDEX idx_shop_locations_shop ON shop_locations(shop_id);
CREATE INDEX idx_shops_display_name ON shops(display_name);
CREATE INDEX idx_shops_franchise ON shops(franchise_uuid);
CREATE INDEX idx_shops_location ON shops(location_id);
CREATE INDEX idx_shops_slug ON shops(slug);
CREATE INDEX idx_shops_type ON shops(shop_type);
CREATE INDEX idx_star_map_locations_jurisdiction ON star_map_locations(jurisdiction_id);
CREATE INDEX idx_star_map_locations_parent ON star_map_locations(parent_uuid);
CREATE INDEX idx_star_map_locations_slug ON star_map_locations(slug);
CREATE INDEX idx_star_map_locations_system ON star_map_locations(star_system_id);
CREATE INDEX idx_star_map_locations_type ON star_map_locations(location_type);
CREATE INDEX idx_terminal_inventory_item ON terminal_inventory(item_uuid);
CREATE INDEX idx_terminal_inventory_terminal ON terminal_inventory(terminal_id);
CREATE INDEX idx_trade_commodities_category ON trade_commodities(category);
CREATE INDEX idx_trade_commodities_slug ON trade_commodities(slug);
CREATE INDEX idx_vehicle_modules_port ON vehicle_modules(vehicle_id, port_name);
CREATE INDEX idx_vehicle_modules_vehicle ON vehicle_modules(vehicle_id);
CREATE INDEX idx_vehicle_modules_version ON vehicle_modules(game_version_id);
CREATE INDEX idx_vehicle_ports_controller ON vehicle_ports(controller);
CREATE INDEX idx_vehicle_ports_parent_port_id ON vehicle_ports(parent_port_id);
CREATE INDEX idx_vehicle_ports_vehicle_id    ON vehicle_ports(vehicle_id);
CREATE INDEX idx_vehicle_suit_lockers_vehicle ON vehicle_suit_lockers(vehicle_id);
CREATE INDEX idx_vehicle_suit_lockers_version ON vehicle_suit_lockers(game_version_id);
CREATE INDEX idx_vehicle_weapon_racks_vehicle ON vehicle_weapon_racks(vehicle_id);
CREATE INDEX idx_vehicle_weapon_racks_version ON vehicle_weapon_racks(game_version_id);
CREATE INDEX idx_vehicles_is_pledgeable ON vehicles(is_pledgeable)
  WHERE is_pledgeable = 0;
CREATE INDEX idx_vehicles_manufacturer_id ON vehicles(manufacturer_id);
CREATE UNIQUE INDEX idx_mining_gadgets_name
  ON mining_gadgets(name);
CREATE UNIQUE INDEX idx_mining_lasers_name
  ON mining_lasers(name);
CREATE UNIQUE INDEX idx_mining_modules_name
  ON mining_modules(name);
CREATE UNIQUE INDEX idx_mining_locations_key
  ON mining_locations(preset_file, name);
CREATE UNIQUE INDEX idx_mining_qd_name
  ON mining_quality_distributions(name);
CREATE UNIQUE INDEX idx_cbrpi_natural_key
  ON crafting_blueprint_reward_pool_items(crafting_blueprint_reward_pool_id, crafting_blueprint_id);
CREATE INDEX idx_component_missiles_cid ON component_missiles(component_id);
CREATE INDEX idx_vehicles_is_variant ON vehicles(is_variant);
CREATE INDEX idx_vehicles_is_purchasable_ingame ON vehicles(is_purchasable_ingame);
CREATE INDEX idx_fps_weapons_has_builtin_scope ON fps_weapons(has_builtin_scope);
CREATE INDEX idx_mrr_mission ON mission_reputation_requirements(mission_id);
CREATE INDEX idx_mrr_faction ON mission_reputation_requirements(faction_slug);
CREATE INDEX idx_mrr_scope   ON mission_reputation_requirements(scope_slug);
CREATE INDEX idx_user_characters_user ON user_characters(user_id);
CREATE INDEX ptu_idx_npc_loadout_items_loadout ON ptu_npc_loadout_items(loadout_id);
CREATE INDEX ptu_idx_npc_loadout_items_item ON ptu_npc_loadout_items(item_name);
CREATE INDEX ptu_idx_npc_loadout_items_version ON ptu_npc_loadout_items(game_version_id);
CREATE INDEX ptu_idx_loot_item_locations_source_key ON ptu_loot_item_locations(source_type, location_key);
CREATE INDEX ptu_idx_loot_item_locations_loot_map ON ptu_loot_item_locations(loot_map_id);
CREATE INDEX ptu_idx_loot_item_locations_version ON ptu_loot_item_locations(game_version_id);
CREATE INDEX ptu_idx_npc_loadout_items_hidden ON ptu_npc_loadout_items(is_hidden);
CREATE INDEX ptu_idx_loot_item_locations_map_source
  ON ptu_loot_item_locations(loot_map_id, source_type);
CREATE INDEX ptu_idx_crafting_blueprints_type ON ptu_crafting_blueprints(type);
CREATE INDEX ptu_idx_crafting_blueprints_sub_type ON ptu_crafting_blueprints(sub_type);
CREATE INDEX ptu_idx_crafting_blueprints_game_version ON ptu_crafting_blueprints(game_version_id);
CREATE INDEX ptu_idx_mining_locations_system ON ptu_mining_locations(system);
CREATE INDEX ptu_idx_mining_locations_game_version ON ptu_mining_locations(game_version_id);
CREATE INDEX ptu_idx_salvageable_ships_base ON ptu_salvageable_ships(base_vehicle_id);
CREATE INDEX ptu_idx_salvageable_ships_version ON ptu_salvageable_ships(game_version_id);
CREATE INDEX ptu_idx_contract_bp_pools_generator ON ptu_contract_blueprint_reward_pools(contract_generator_key);
CREATE INDEX ptu_idx_contract_bp_pools_pool ON ptu_contract_blueprint_reward_pools(crafting_blueprint_reward_pool_id);
CREATE INDEX ptu_idx_contract_bp_pools_version ON ptu_contract_blueprint_reward_pools(game_version_id);
CREATE INDEX ptu_idx_cgbp_contract_id ON ptu_contract_generator_blueprint_pools(contract_generator_contract_id);
CREATE INDEX ptu_idx_terminals_shop ON ptu_terminals(shop_id);
CREATE INDEX ptu_idx_terminals_type ON ptu_terminals(terminal_type);
CREATE INDEX ptu_idx_terminals_uex ON ptu_terminals(uex_terminal_id);
CREATE INDEX ptu_idx_component_coolers_cid ON ptu_component_coolers(component_id);
CREATE INDEX ptu_idx_component_mining_cid ON ptu_component_mining(component_id);
CREATE INDEX ptu_idx_component_powerplants_cid ON ptu_component_powerplants(component_id);
CREATE INDEX ptu_idx_component_qed_cid ON ptu_component_qed(component_id);
CREATE INDEX ptu_idx_component_quantum_drives_cid ON ptu_component_quantum_drives(component_id);
CREATE INDEX ptu_idx_component_radar_cid ON ptu_component_radar(component_id);
CREATE INDEX ptu_idx_component_shields_cid ON ptu_component_shields(component_id);
CREATE INDEX ptu_idx_component_thrusters_cid ON ptu_component_thrusters(component_id);
CREATE INDEX ptu_idx_component_turrets_cid ON ptu_component_turrets(component_id);
CREATE INDEX ptu_idx_component_weapons_cid ON ptu_component_weapons(component_id);
CREATE INDEX ptu_idx_consumable_effects_uuid ON ptu_consumable_effects(consumable_uuid);
CREATE INDEX ptu_idx_consumable_effects_version ON ptu_consumable_effects(game_version_id);
CREATE INDEX ptu_idx_consumables_slug     ON ptu_consumables(slug);
CREATE INDEX ptu_idx_consumables_sub_type ON ptu_consumables(sub_type);
CREATE INDEX ptu_idx_consumables_type     ON ptu_consumables(type);
CREATE UNIQUE INDEX ptu_idx_cgbp_upsert
  ON ptu_contract_generator_blueprint_pools(contract_generator_contract_id, crafting_blueprint_reward_pool_id);
CREATE UNIQUE INDEX ptu_idx_cgc_upsert
  ON ptu_contract_generator_careers(debug_name, contract_generator_id);
CREATE INDEX ptu_idx_cgc_career_id ON ptu_contract_generator_contracts(career_id);
CREATE INDEX ptu_idx_cgc_desc_loc_key ON ptu_contract_generator_contracts(desc_loc_key);
CREATE INDEX ptu_idx_cg_faction_slug ON ptu_contract_generators(faction_slug, game_version_id);
CREATE INDEX ptu_idx_cg_generator_key ON ptu_contract_generators(generator_key);
CREATE INDEX ptu_idx_contracts_giver_slug ON ptu_contracts(giver_slug);
CREATE INDEX ptu_idx_contracts_is_active  ON ptu_contracts(is_active);
CREATE INDEX ptu_idx_crafting_bp_reward_pools_version ON ptu_crafting_blueprint_reward_pools(game_version_id);
CREATE INDEX ptu_idx_faction_reputation_scopes_faction ON ptu_faction_reputation_scopes(faction_id);
CREATE INDEX ptu_idx_faction_reputation_scopes_scope ON ptu_faction_reputation_scopes(reputation_scope_id);
CREATE INDEX ptu_idx_faction_reputation_scopes_version ON ptu_faction_reputation_scopes(game_version_id);
CREATE INDEX ptu_idx_factions_slug ON ptu_factions(slug);
CREATE INDEX ptu_idx_factions_type ON ptu_factions(faction_type);
CREATE INDEX ptu_idx_fps_ammo_types_version ON ptu_fps_ammo_types(game_version_id);
CREATE INDEX ptu_idx_fps_carryables_version ON ptu_fps_carryables(game_version_id);
CREATE INDEX ptu_idx_fps_clothing_slot ON ptu_fps_clothing(slot);
CREATE INDEX ptu_idx_fps_clothing_slug ON ptu_fps_clothing(slug);
CREATE INDEX ptu_idx_fps_helmets_slug     ON ptu_fps_helmets(slug);
CREATE INDEX ptu_idx_fps_helmets_sub_type ON ptu_fps_helmets(sub_type);
CREATE INDEX ptu_idx_fps_melee_manufacturer ON ptu_fps_melee(manufacturer_id);
CREATE INDEX ptu_idx_fps_melee_version ON ptu_fps_melee(game_version_id);
CREATE INDEX ptu_idx_harvestables_slug ON ptu_harvestables(slug);
CREATE INDEX ptu_idx_jio_infraction ON ptu_jurisdiction_infraction_overrides(infraction_id);
CREATE INDEX ptu_idx_jio_jurisdiction ON ptu_jurisdiction_infraction_overrides(jurisdiction_id);
CREATE INDEX ptu_idx_law_infractions_severity ON ptu_law_infractions(severity);
CREATE INDEX ptu_idx_law_infractions_slug ON ptu_law_infractions(slug);
CREATE INDEX ptu_idx_law_jurisdictions_slug ON ptu_law_jurisdictions(slug);
CREATE INDEX ptu_idx_loot_map_category ON ptu_loot_map(category);
CREATE INDEX ptu_idx_loot_map_class_name ON ptu_loot_map(class_name);
CREATE INDEX ptu_idx_loot_map_consumable   ON ptu_loot_map(consumable_id);
CREATE INDEX ptu_idx_loot_map_fps_armour_id ON ptu_loot_map(fps_armour_id);
CREATE INDEX ptu_idx_loot_map_fps_carryable ON ptu_loot_map(fps_carryable_id);
CREATE INDEX ptu_idx_loot_map_fps_clothing_id ON ptu_loot_map(fps_clothing_id);
CREATE INDEX ptu_idx_loot_map_fps_helmet_id ON ptu_loot_map(fps_helmet_id);
CREATE INDEX ptu_idx_loot_map_fps_melee ON ptu_loot_map(fps_melee_id);
CREATE INDEX ptu_idx_loot_map_fps_weapon_id ON ptu_loot_map(fps_weapon_id);
CREATE INDEX ptu_idx_loot_map_game_version ON ptu_loot_map(game_version_id);
CREATE INDEX ptu_idx_loot_map_harvestable  ON ptu_loot_map(harvestable_id);
CREATE INDEX ptu_idx_loot_map_props        ON ptu_loot_map(props_id);
CREATE INDEX ptu_idx_loot_map_rarity       ON ptu_loot_map(rarity);
CREATE INDEX ptu_idx_loot_map_ship_missile ON ptu_loot_map(ship_missile_id);
CREATE INDEX ptu_idx_loot_map_sub_type     ON ptu_loot_map(sub_type);
CREATE INDEX ptu_idx_loot_map_type         ON ptu_loot_map(type);
CREATE INDEX ptu_idx_loot_map_uuid_gv ON ptu_loot_map(uuid, game_version_id);
CREATE INDEX ptu_idx_loot_map_vehicle_component_id ON ptu_loot_map(vehicle_component_id);
CREATE INDEX ptu_idx_mission_givers_faction ON ptu_mission_givers(faction_id);
CREATE INDEX ptu_idx_mission_givers_location ON ptu_mission_givers(location_id);
CREATE INDEX ptu_idx_mission_givers_slug ON ptu_mission_givers(slug);
CREATE INDEX ptu_idx_mission_organizations_slug ON ptu_mission_organizations(slug);
CREATE INDEX ptu_idx_mp_required ON ptu_mission_prerequisites(required_mission_id);
CREATE INDEX ptu_idx_mission_types_category ON ptu_mission_types(category);
CREATE INDEX ptu_idx_missions_category ON ptu_missions(category);
CREATE INDEX ptu_idx_missions_giver ON ptu_missions(mission_giver_id);
CREATE INDEX ptu_idx_missions_reputation ON ptu_missions(reputation_scope_id);
CREATE INDEX ptu_idx_missions_type ON ptu_missions(mission_type_id);
CREATE INDEX ptu_idx_missions_version ON ptu_missions(game_version_id);
CREATE INDEX ptu_idx_npc_loadouts_faction ON ptu_npc_loadouts(faction_id);
CREATE INDEX ptu_idx_npc_loadouts_version ON ptu_npc_loadouts(game_version_id);
CREATE INDEX ptu_idx_paints_class_name ON ptu_paints(class_name);
CREATE INDEX ptu_idx_paints_slug ON ptu_paints(slug);
CREATE INDEX ptu_idx_paints_version ON ptu_paints(game_version_id);
CREATE INDEX ptu_idx_props_slug     ON ptu_props(slug);
CREATE INDEX ptu_idx_props_sub_type ON ptu_props(sub_type);
CREATE INDEX ptu_idx_reputation_perks_scope ON ptu_reputation_perks(scope_id);
CREATE INDEX ptu_idx_reputation_perks_version ON ptu_reputation_perks(game_version_id);
CREATE INDEX ptu_idx_reputation_reward_tiers_version ON ptu_reputation_reward_tiers(game_version_id);
CREATE INDEX ptu_idx_reputation_scopes_key ON ptu_reputation_scopes(scope_key);
CREATE INDEX ptu_idx_reputation_standings_scope ON ptu_reputation_standings(scope_id);
CREATE UNIQUE INDEX ptu_idx_salvageable_ships_uuid_version ON ptu_salvageable_ships(entity_uuid);
CREATE INDEX ptu_idx_ship_missiles_size ON ptu_ship_missiles(size);
CREATE INDEX ptu_idx_ship_missiles_type ON ptu_ship_missiles(type, sub_type);
CREATE INDEX ptu_idx_ship_missiles_uuid ON ptu_ship_missiles(uuid);
CREATE INDEX ptu_idx_shop_locations_location ON ptu_shop_locations(location_id);
CREATE INDEX ptu_idx_shop_locations_shop ON ptu_shop_locations(shop_id);
CREATE INDEX ptu_idx_shops_display_name ON ptu_shops(display_name);
CREATE INDEX ptu_idx_shops_franchise ON ptu_shops(franchise_uuid);
CREATE INDEX ptu_idx_shops_location ON ptu_shops(location_id);
CREATE INDEX ptu_idx_shops_slug ON ptu_shops(slug);
CREATE INDEX ptu_idx_shops_type ON ptu_shops(shop_type);
CREATE INDEX ptu_idx_star_map_locations_jurisdiction ON ptu_star_map_locations(jurisdiction_id);
CREATE INDEX ptu_idx_star_map_locations_parent ON ptu_star_map_locations(parent_uuid);
CREATE INDEX ptu_idx_star_map_locations_slug ON ptu_star_map_locations(slug);
CREATE INDEX ptu_idx_star_map_locations_system ON ptu_star_map_locations(star_system_id);
CREATE INDEX ptu_idx_star_map_locations_type ON ptu_star_map_locations(location_type);
CREATE INDEX ptu_idx_trade_commodities_category ON ptu_trade_commodities(category);
CREATE INDEX ptu_idx_trade_commodities_slug ON ptu_trade_commodities(slug);
CREATE INDEX ptu_idx_vehicle_modules_port ON ptu_vehicle_modules(vehicle_id, port_name);
CREATE INDEX ptu_idx_vehicle_modules_vehicle ON ptu_vehicle_modules(vehicle_id);
CREATE INDEX ptu_idx_vehicle_modules_version ON ptu_vehicle_modules(game_version_id);
CREATE INDEX ptu_idx_vehicle_ports_controller ON ptu_vehicle_ports(controller);
CREATE INDEX ptu_idx_vehicle_ports_parent_port_id ON ptu_vehicle_ports(parent_port_id);
CREATE INDEX ptu_idx_vehicle_ports_vehicle_id    ON ptu_vehicle_ports(vehicle_id);
CREATE INDEX ptu_idx_vehicle_suit_lockers_vehicle ON ptu_vehicle_suit_lockers(vehicle_id);
CREATE INDEX ptu_idx_vehicle_suit_lockers_version ON ptu_vehicle_suit_lockers(game_version_id);
CREATE INDEX ptu_idx_vehicle_weapon_racks_vehicle ON ptu_vehicle_weapon_racks(vehicle_id);
CREATE INDEX ptu_idx_vehicle_weapon_racks_version ON ptu_vehicle_weapon_racks(game_version_id);
CREATE INDEX ptu_idx_vehicles_is_pledgeable ON ptu_vehicles(is_pledgeable)
  WHERE is_pledgeable = 0;
CREATE INDEX ptu_idx_vehicles_manufacturer_id ON ptu_vehicles(manufacturer_id);
CREATE UNIQUE INDEX ptu_idx_lil_natural_key
  ON ptu_loot_item_locations(loot_map_id, source_type, location_key, COALESCE(location_label, ''));
CREATE UNIQUE INDEX ptu_idx_nli_natural_key
  ON ptu_npc_loadout_items(loadout_id, item_name, COALESCE(slot, ''));
CREATE UNIQUE INDEX ptu_idx_mining_gadgets_name
  ON ptu_mining_gadgets(name);
CREATE UNIQUE INDEX ptu_idx_mining_lasers_name
  ON ptu_mining_lasers(name);
CREATE UNIQUE INDEX ptu_idx_mining_modules_name
  ON ptu_mining_modules(name);
CREATE UNIQUE INDEX ptu_idx_mining_locations_key
  ON ptu_mining_locations(preset_file, name);
CREATE UNIQUE INDEX ptu_idx_mining_qd_name
  ON ptu_mining_quality_distributions(name);
CREATE INDEX ptu_idx_component_missiles_cid ON ptu_component_missiles(component_id);
CREATE INDEX ptu_idx_vehicles_is_variant ON ptu_vehicles(is_variant);
CREATE INDEX ptu_idx_vehicles_is_purchasable_ingame ON ptu_vehicles(is_purchasable_ingame);
CREATE INDEX ptu_idx_fps_weapons_has_builtin_scope ON ptu_fps_weapons(has_builtin_scope);
CREATE INDEX ptu_idx_mrr_mission ON ptu_mission_reputation_requirements(mission_id);
CREATE INDEX ptu_idx_mrr_faction ON ptu_mission_reputation_requirements(faction_slug);
CREATE INDEX ptu_idx_mrr_scope   ON ptu_mission_reputation_requirements(scope_slug);
CREATE UNIQUE INDEX idx_crafting_blueprint_slots_unique
  ON crafting_blueprint_slots(crafting_blueprint_id, slot_index);
CREATE UNIQUE INDEX idx_crafting_slot_modifiers_unique
  ON crafting_slot_modifiers(crafting_blueprint_slot_id, crafting_property_id);
CREATE INDEX ptu_idx_crafting_blueprint_slots_blueprint
  ON ptu_crafting_blueprint_slots(crafting_blueprint_id);
CREATE UNIQUE INDEX idx_ptu_crafting_blueprint_slots_unique
  ON ptu_crafting_blueprint_slots(crafting_blueprint_id, slot_index);
CREATE INDEX ptu_idx_crafting_slot_modifiers_slot
  ON ptu_crafting_slot_modifiers(crafting_blueprint_slot_id);
CREATE UNIQUE INDEX idx_ptu_crafting_slot_modifiers_unique
  ON ptu_crafting_slot_modifiers(crafting_blueprint_slot_id, crafting_property_id);
CREATE INDEX idx_vehicles_is_npc_only ON vehicles(is_npc_only);
CREATE INDEX ptu_idx_vehicles_is_npc_only ON ptu_vehicles(is_npc_only);
CREATE INDEX idx_ptu_crafting_bp_reward_items_pool
    ON ptu_crafting_blueprint_reward_pool_items(crafting_blueprint_reward_pool_id);
CREATE INDEX idx_ptu_crafting_bp_reward_items_bp
    ON ptu_crafting_blueprint_reward_pool_items(crafting_blueprint_id);
CREATE UNIQUE INDEX idx_ptu_crafting_bp_reward_items_unique
    ON ptu_crafting_blueprint_reward_pool_items(crafting_blueprint_reward_pool_id, crafting_blueprint_id);
CREATE UNIQUE INDEX idx_user_blueprints_user_uuid
  ON user_blueprints(user_id, blueprint_uuid);
CREATE INDEX idx_user_blueprints_owned
  ON user_blueprints(user_id, is_owned) WHERE is_owned = 1;
CREATE INDEX idx_user_blueprints_wishlist
  ON user_blueprints(user_id, is_wishlist) WHERE is_wishlist = 1;
CREATE UNIQUE INDEX idx_user_fleet_user_pledge_vehicle
  ON user_fleet(user_id, pledge_id, vehicle_id)
  WHERE pledge_id IS NOT NULL;
CREATE INDEX idx_user_loot_collection_user ON user_loot_collection(user_id);
CREATE INDEX idx_user_loot_collection_uuid ON user_loot_collection(loot_uuid);
CREATE UNIQUE INDEX idx_user_loot_collection_user_uuid
  ON user_loot_collection(user_id, loot_uuid);
CREATE INDEX idx_user_loot_wishlist_user ON user_loot_wishlist(user_id);
CREATE INDEX idx_user_loot_wishlist_uuid ON user_loot_wishlist(loot_uuid);
CREATE UNIQUE INDEX idx_user_loot_wishlist_user_uuid
  ON user_loot_wishlist(user_id, loot_uuid);
CREATE INDEX idx_user_blueprint_builds_user
  ON user_blueprint_builds(user_id);
CREATE INDEX idx_user_blueprint_builds_uuid
  ON user_blueprint_builds(blueprint_uuid);
CREATE UNIQUE INDEX idx_user_blueprint_builds_unique
  ON user_blueprint_builds(user_id, blueprint_uuid, name);
CREATE UNIQUE INDEX idx_pledge_item_media_title_lower
  ON pledge_item_media(title_lower);
CREATE INDEX idx_image_captures_title_norm   ON image_captures(title_norm);
CREATE INDEX idx_paints_title_norm           ON paints(title_norm);
CREATE INDEX idx_pledge_item_media_title_norm ON pledge_item_media(title_norm);
CREATE INDEX idx_image_captures_manual_paint_id
  ON image_captures(manual_paint_id);
CREATE INDEX idx_image_captures_manual_match
  ON image_captures(manual_match_kind, manual_match_id);
CREATE INDEX idx_mineable_elements_category ON mineable_elements(category);
CREATE INDEX idx_mineable_elements_slug     ON mineable_elements(slug);
CREATE INDEX ptu_idx_mineable_elements_category ON ptu_mineable_elements(category);
CREATE INDEX ptu_idx_mineable_elements_slug     ON ptu_mineable_elements(slug);
CREATE UNIQUE INDEX idx_nli_natural_key
  ON npc_loadout_items(loadout_id, item_name, COALESCE(slot, ''));
CREATE UNIQUE INDEX idx_lil_natural_key
  ON loot_item_locations(loot_map_id, source_type, location_key, COALESCE(location_label, ''));
CREATE INDEX idx_npc_loadout_items_loadout_hidden
  ON npc_loadout_items(loadout_id, is_hidden);
CREATE INDEX idx_vehicle_storage_vehicle ON vehicle_storage(vehicle_id);
CREATE INDEX idx_vehicle_storage_type ON vehicle_storage(storage_type);
CREATE INDEX idx_ptu_vehicle_storage_vehicle ON ptu_vehicle_storage(vehicle_id);
CREATE INDEX idx_ptu_vehicle_storage_type ON ptu_vehicle_storage(storage_type);
CREATE INDEX idx_mission_rep_changes_mission ON mission_rep_changes(mission_id);
CREATE INDEX idx_mission_rep_changes_scope ON mission_rep_changes(scope_slug);
CREATE INDEX idx_user_localization_overrides_user ON user_localization_overrides(user_id);
CREATE INDEX idx_terminal_inventory_uex_modified
  ON terminal_inventory(uex_date_modified)
  WHERE uex_date_modified IS NOT NULL;
CREATE INDEX ptu_idx_terminal_inventory_item
  ON ptu_terminal_inventory(item_uuid);
CREATE INDEX ptu_idx_terminal_inventory_terminal
  ON ptu_terminal_inventory(terminal_id);
CREATE INDEX ptu_idx_terminal_inventory_uex_modified
  ON ptu_terminal_inventory(uex_date_modified)
  WHERE uex_date_modified IS NOT NULL;
CREATE INDEX idx_rock_compositions_deposit_name
  ON rock_compositions(deposit_name)
  WHERE deposit_name IS NOT NULL;
CREATE INDEX ptu_idx_rock_compositions_deposit_name
  ON ptu_rock_compositions(deposit_name)
  WHERE deposit_name IS NOT NULL;
CREATE INDEX ptu_idx_rock_compositions_rock_type
  ON ptu_rock_compositions(rock_type);
CREATE INDEX idx_mineable_rock_entities_composition
  ON mineable_rock_entities(composition_uuid);
CREATE INDEX idx_mineable_rock_entities_class
  ON mineable_rock_entities(class_name);
CREATE INDEX idx_mineable_rock_entities_category
  ON mineable_rock_entities(rock_category);
CREATE INDEX ptu_idx_mineable_rock_entities_composition
  ON ptu_mineable_rock_entities(composition_uuid);
CREATE INDEX ptu_idx_mineable_rock_entities_class
  ON ptu_mineable_rock_entities(class_name);
CREATE INDEX ptu_idx_mineable_rock_entities_category
  ON ptu_mineable_rock_entities(rock_category);
CREATE INDEX idx_component_fuel_nozzles_cid ON component_fuel_nozzles(component_id);
CREATE INDEX idx_ai_chats_user ON ai_chats(user_id, updated_at);
CREATE INDEX idx_ai_chat_messages_chat ON ai_chat_messages(chat_id, id);
-- ── data ──
INSERT INTO vehicle_types VALUES(1,'spaceship','Spaceship');
INSERT INTO vehicle_types VALUES(2,'ground_vehicle','Ground Vehicle');
INSERT INTO vehicle_types VALUES(3,'gravlev','Gravlev');
INSERT INTO vehicle_types VALUES(4,'powersuit','Powersuit');
INSERT INTO insurance_types VALUES(1,'lti','Lifetime Insurance',NULL,1);
INSERT INTO insurance_types VALUES(2,'120_month','120-Month Insurance',120,0);
INSERT INTO insurance_types VALUES(3,'72_month','72-Month Insurance',72,0);
INSERT INTO insurance_types VALUES(4,'6_month','6-Month Insurance',6,0);
INSERT INTO insurance_types VALUES(5,'3_month','3-Month Insurance',3,0);
INSERT INTO insurance_types VALUES(6,'standard','Standard Insurance',NULL,0);
INSERT INTO insurance_types VALUES(7,'unknown','Unknown Insurance',NULL,0);
INSERT INTO insurance_types VALUES(8,'60_month','60-Month Insurance',60,0);
INSERT INTO insurance_types VALUES(9,'2_month','2-Month Insurance',2,0);
INSERT INTO sync_sources VALUES(1,'scwiki','SC Wiki API');
INSERT INTO sync_sources VALUES(2,'fleetyards','FleetYards (Images Only)');
INSERT INTO sync_sources VALUES(3,'hangarxplor','HangarXplor');
INSERT INTO sync_sources VALUES(4,'scunpacked','scunpacked-data (Paints)');
INSERT INTO sync_sources VALUES(5,'rsi_api','RSI API (Images)');
INSERT INTO production_statuses VALUES(1,'flight_ready','Flight Ready');
INSERT INTO production_statuses VALUES(2,'in_production','In Production');
INSERT INTO production_statuses VALUES(3,'in_concept','In Concept');
INSERT INTO production_statuses VALUES(4,'unknown','Unknown');
INSERT INTO game_versions VALUES(1,'11319298-4600-0000-0000-000000000001','4.6.0-live.11319298','LIVE',1,'2026-02-25','2026-06-17 21:55:35','2026-06-17 21:55:35','11319298');
INSERT INTO game_versions VALUES(2,'test-0000-0000-0000-000000000001','4.0.0-test','LIVE',1,'2026-01-01','2026-06-17 21:55:39','2026-06-17 21:55:39',NULL);
INSERT INTO change_event_types VALUES(1,'provider_linked','Provider Linked','auth');
INSERT INTO change_event_types VALUES(2,'provider_unlinked','Provider Unlinked','auth');
INSERT INTO change_event_types VALUES(3,'password_set','Password Set','auth');
INSERT INTO change_event_types VALUES(4,'password_changed','Password Changed','auth');
INSERT INTO change_event_types VALUES(5,'profile_updated','Profile Updated','profile');
INSERT INTO change_event_types VALUES(6,'email_changed','Email Changed','profile');
INSERT INTO change_event_types VALUES(7,'2fa_enabled','2FA Enabled','auth');
INSERT INTO change_event_types VALUES(8,'2fa_disabled','2FA Disabled','auth');
INSERT INTO change_event_types VALUES(9,'passkey_added','Passkey Added','auth');
INSERT INTO change_event_types VALUES(10,'passkey_removed','Passkey Removed','auth');
INSERT INTO change_event_types VALUES(11,'passkey_renamed','Passkey Renamed','auth');
INSERT INTO change_event_types VALUES(12,'session_revoked','Session Revoked','session');
INSERT INTO change_event_types VALUES(13,'account_deleted','Account Deleted','account');
INSERT INTO change_event_types VALUES(14,'fleet_imported','Fleet Imported','fleet');
INSERT INTO change_event_types VALUES(15,'settings_changed','Settings Changed','settings');
INSERT INTO change_event_types VALUES(16,'llm_config_changed','LLM Config Changed','settings');
INSERT INTO change_event_types VALUES(17,'account_suspended','Account Suspended','account');
INSERT INTO change_event_types VALUES(18,'account_banned','Account Banned','account');
INSERT INTO change_event_types VALUES(19,'account_reinstated','Account Reinstated','account');
INSERT INTO change_event_types VALUES(20,'hangar_synced','Hangar Synced via Extension','fleet');
INSERT INTO crafting_properties VALUES(1,'weapon_recoil_handling','Recoil Handling','%+.2f %%','weapon');
INSERT INTO crafting_properties VALUES(2,'weapon_reloadspeed','Reload Speed',NULL,'weapon');
INSERT INTO crafting_properties VALUES(3,'weapon_recoil_smoothness','Recoil Smoothness','%+.2f %%','weapon');
INSERT INTO crafting_properties VALUES(4,'weapon_firerate','Fire Rate','%.2f RPM','weapon');
INSERT INTO crafting_properties VALUES(5,'weapon_spread','Spread',NULL,'weapon');
INSERT INTO crafting_properties VALUES(6,'weapon_damage','Impact Force','%+.2f %%','weapon');
INSERT INTO crafting_properties VALUES(7,'weapon_recoil_kick','Recoil Kick','%+.2f %%','weapon');
INSERT INTO crafting_properties VALUES(8,'armor_damagemitigation','Damage Mitigation',NULL,'armor');
INSERT INTO crafting_properties VALUES(9,'armor_radiationcapacity','Radiation Capacity','%.2f mRem','armor');
INSERT INTO crafting_properties VALUES(10,'armor_radiationdissipation','Radiation Dissipation','%.2f mRem/s','armor');
INSERT INTO crafting_properties VALUES(11,'armor_temperaturemin','Min Temp','%+.2f °C','armor');
INSERT INTO crafting_properties VALUES(12,'armor_temperaturemax','Max Temp','%+.2f °C','armor');
INSERT INTO crafting_properties VALUES(13,'crafter_craftspeed','Craft Speed',NULL,'crafter');
INSERT INTO crafting_properties VALUES(14,'crafter_dismantleefficiency','Dismantle Efficiency',NULL,'crafter');
INSERT INTO op_types VALUES(1,'mining','Mining Run');
INSERT INTO op_types VALUES(2,'cargo','Cargo Haul');
INSERT INTO op_types VALUES(3,'bounty','Bounty Hunting');
INSERT INTO op_types VALUES(4,'salvage','Salvage Op');
INSERT INTO op_types VALUES(5,'escort','Escort Mission');
INSERT INTO op_types VALUES(6,'exploration','Exploration');
INSERT INTO op_types VALUES(7,'trade','Trade Run');
INSERT INTO op_types VALUES(8,'other','Other');
INSERT INTO rating_categories VALUES(1,'reliability','Reliability','Shows up on time, follows through');
INSERT INTO rating_categories VALUES(2,'skill','Skill','Competent at their role');
INSERT INTO rating_categories VALUES(3,'communication','Communication','Responsive, clear, keeps team informed');
INSERT INTO rating_categories VALUES(4,'fairness','Fairness','Honest, fair dealings, trustworthy with money');
INSERT INTO manufacturers VALUES(1,'00000000-0000-0000-0000-000000000001','Generic','generic','GENERIC',NULL,'Unbranded / commodity gear with no specific consumer manufacturer in p4k. Assigned by the extractor when tag-, AttachDef-, filename-segment, and static-prefix lookups all return None.',NULL,NULL,1,'2026-06-17 21:55:38','2026-06-17 21:55:38',NULL,0,NULL,0,NULL,NULL);
